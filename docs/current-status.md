# Текущее состояние проекта по Roadmap

## ✅ Выполнено

### Этап 0. Подготовка инфраструктуры
- ✅ Docker Compose конфигурация
- ✅ MySQL с PostGIS
- ✅ Redis
- ✅ Backend проект (Node.js + Express)
- ✅ Telegram Mini App (Vue 3 + Vite)
- ⚠️ Admin Panel - не создан (отдельный проект)

### Этап 1. Backend: Базовая архитектура
- ✅ Структура БД (все таблицы, включая варианты и группы модификаторов)
- ✅ Express сервер
- ✅ Подключение к MySQL и Redis
- ✅ Middleware для ошибок и CORS
- ✅ Система аутентификации (JWT, Telegram)

### Этап 2. Backend: Управление пользователями
- ✅ API пользователей (регистрация, профиль, адреса)
- ✅ Система ролей и доступов

### Этап 3. Backend: Справочники
- ✅ API городов и филиалов
- ⚠️ API меню - частично:
  - ✅ GET /api/menu/categories
  - ✅ GET /api/menu/items?category_id={id}
  - ✅ GET /api/menu/items/{id} (но без вариантов и групп модификаторов)
  - ✅ Админские эндпоинты для категорий и позиций
  - ❌ GET /api/menu?city_id={id} - полное меню города
  - ❌ API для вариантов позиций (item_variants)
  - ❌ API для групп модификаторов (modifier_groups)
- ⚠️ API полигонов:
  - ✅ GET /api/polygons/city/:cityId
  - ✅ POST /api/polygons/check-delivery
  - ❌ POST /api/geocode - геокодирование

### Этап 4. Backend: Заказы
- ✅ POST /api/orders - создание заказа
- ⚠️ Нужно обновить для поддержки variant_id

### Этап 5. Backend: Программа лояльности
- ✅ Базовая логика бонусов
- ❌ Автоматическое повышение уровня (checkLevelUp)

## 🔄 Текущий этап: Этап 3.2 - API меню

### Что нужно сделать:

1. **Добавить GET /api/menu** - полное меню города с вариантами и группами модификаторов
2. **Обновить GET /api/menu/items/{id}** - включить варианты и группы модификаторов
3. **Добавить API для вариантов позиций:**
   - GET /api/menu/items/:id/variants
   - POST /api/admin/menu/items/:id/variants
   - PUT /api/admin/menu/variants/:id
   - DELETE /api/admin/menu/variants/:id
4. **Добавить API для групп модификаторов:**
   - GET /api/menu/modifier-groups
   - GET /api/menu/modifier-groups/:id
   - POST /api/admin/menu/modifier-groups
   - PUT /api/admin/menu/modifier-groups/:id
   - DELETE /api/admin/menu/modifier-groups/:id
5. **Обновить POST /api/admin/menu/items** - поддержка создания вариантов
6. **Добавить POST /api/geocode** - геокодирование адресов

## 📋 План работы

1. ✅ Обновлена БД (варианты и группы модификаторов)
2. 🔄 Реализация API для вариантов позиций
3. 🔄 Реализация API для групп модификаторов
4. 🔄 Обновление GET /api/menu для включения вариантов
5. 🔄 Добавление геокодирования
6. 🔄 Обновление логики заказов для вариантов
7. 🔄 Реализация автоповышения уровня лояльности
