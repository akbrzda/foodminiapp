# Техническое задание: Система управления меню

## ПРИ РАЗРАБОТКА ИНТЕРФЕЙСА КЛИЕНТСКОЙ ЧАСТИ, ОБЯЗАТЕЛЬНО ПРИДЕРЖИВАТЬСЯ ТЕКУЩЕЙ ДИЗАЙН-СИСТЕМЫ. НА ОСНОВЕ НЕГО ДАЛЬШЕ РАЗРАБОТАТЬ ДРУГИЕ КОМПОНЕНТЫ.

## 1. Описание и возможности системы меню

### 1.1 Общая концепция

Система управления меню предназначена для создания гибкого, мультигородного меню ресторанов с возможностью:

- Управления блюдами с различными вариациями (размеры, порции)
- Гибких модификаторов (дополнения, соусы, ингредиенты)
- Дифференцированного ценообразования (по городам, филиалам, способам получения)
- Временного отключения позиций (стоп-лист)
- Фильтрации и поиска по тегам

### 1.2 Основные сущности

#### 1.2.1 Категория (Category)

**Описание**: Группировка блюд для удобной навигации в меню.
**Свойства**:

- Название
- Описание (опционально)
- Порядок отображения
- Активность (включено/выключено)
- Привязка к городам
  **Особенности**:
- Категории глобальные (создаются один раз)
- Можно включать/отключать для конкретных городов
- Без иерархии (нет подкатегорий)
- Порядок можно задавать вручную или использовать автоматический
  **Примеры**: "Пицца", "Суши", "Напитки", "Десерты"

---

#### 1.2.2 Блюдо (Menu Item)

**Описание**: Основная позиция меню.
**Свойства**:

- Название
- Описание
- Состав (текст)
- КБЖУ (калории, белки, жиры, углеводы) - на 100г и на порцию
- Вес (с единицей измерения: г, кг, мл, л, шт)
- Цена базовая
- Фотография (одна)
- Активность
- Привязка к категориям (множественная)
- Привязка к городам
- Привязка к способам получения (доставка, самовывоз, зал)
  **Особенности**:
- Блюдо может находиться в нескольких категориях одновременно
- Блюдо глобальное, но можно включать/отключать по городам
- Цены могут отличаться для разных:
  - Городов
  - Способов получения (доставка/самовывоз/зал)
- Стоп-лист на уровне филиалов
- КБЖУ указывается на 100г и на всю порцию
  **Примеры**:
- "Маргарита" (может быть в категориях "Пицца" и "Вегетарианское")
- "Том Ям" (только в категории "Супы")

---

#### 1.2.3 Вариация блюда (Variant)

**Описание**: Различные варианты одного блюда (размеры, количество порций).
**Свойства**:

- Название (например: "23 см", "Большая порция", "500 мл")
- Цена
- Вес (с единицей измерения)
- КБЖУ (собственные, отличные от базовых)
- Фото (пока берется из основного блюда, в будущем можно будет загружать свое)
  **Особенности**:
- Если у блюда добавлены вариации, они становятся **обязательными** для выбора
- Без вариаций заказать блюдо нельзя
- Обычно отражают размер или количество порций
- КБЖУ для каждой вариации свои (пересчитываются на вес вариации)
- Цены вариаций также могут отличаться по городам и способам получения
  **Примеры**:
- Пицца "Маргарита": 23 см - 599₽, 28 см - 699₽, 33 см - 899₽
- Том Ям: 350 мл - 450₽, 500 мл - 650₽
- Картофель фри: Маленькая - 150₽, Средняя - 200₽, Большая - 250₽

---

#### 1.2.4 Группа модификаторов (Modifier Group)

**Описание**: Логическая группа модификаторов с правилами выбора.
**Свойства**:

- Название группы (например: "Соусы", "Топпинги", "Основа для пиццы")
- Тип: обязательная / необязательная
- Минимальное количество выборов (по умолчанию: обязательные = 1, необязательные = 0)
- Максимальное количество выборов (диапазон)
- Глобальность (переиспользуемая или уникальная для блюда)
  **Особенности**:
- Группы могут быть **глобальными** (создаются один раз, переиспользуются) или **уникальными** (для конкретного блюда)
- **Обязательные группы**: без выбора модификатора из этой группы клиент не сможет заказать блюдо
- **Необязательные группы**: клиент может их пропустить
- Количество выборов задается диапазоном (например: от 1 до 3)
  **Примеры**:
- "Основа для пиццы" (обязательная, выбор 1 из: традиционное, тонкое)
- "Соусы" (необязательная, выбор 0-3 из: кетчуп, майонез, горчица, барбекю)
- "Дополнительные топпинги" (необязательная, выбор 0-5)

---

#### 1.2.5 Модификатор (Modifier)

**Описание**: Конкретное дополнение или изменение блюда.
**Свойства**:

- Название
- Цена (может быть 0₽ или положительной, отрицательных цен нет)
- Вес (опционально, с единицей измерения)
- Фотография
- Привязка к группе модификаторов
- Привязка к вариациям (цены могут меняться в зависимости от выбранной вариации)
  **Особенности**:
- Модификаторы могут быть **глобальными** (переиспользуемыми) или **уникальными**
- При добавлении глобальной группы модификаторов к блюду:
  - Можно включать/отключать отдельные модификаторы из группы
  - Цены остаются глобальными (не изменяются для конкретного блюда)
- **Цены и вес модификаторов могут зависеть от выбранной вариации**:
  - Пример: Пицца 23 см + Моцарелла = +50₽
  - Пицца 28 см + Моцарелла = +80₽
- Настройка цен по вариациям может быть:
  - **Ручная**: для каждой вариации указывается своя цена
  - **Автоматическая**: цена рассчитывается как процент от цены первой вариации
    **Примеры**:
- "Кетчуп" - 0₽ (бесплатный)
- "Сыр Моцарелла" - от 50₽ до 150₽ (в зависимости от размера пиццы)
- "Тонкое тесто" - 0₽
- "Двойная порция креветок" - +300₽

---

#### 1.2.6 Тег (Tag)

**Описание**: Метка для фильтрации и поиска блюд.
**Свойства**:

- Название
- Иконка/цвет (опционально)
  **Особенности**:
- Теги глобальные (создаются один раз, переиспользуются)
- Используются для фильтрации в клиентской части
- Можно добавлять новые теги через админ-панель
  **Примеры**: "Острое", "Веган", "Без глютена", "Хит продаж", "Новинка"

---

### 1.3 Связи между сущностями

```
Категория (1) ←→ (N) Блюдо (N) ←→ (N) Категория
    ↓
Блюдо (1) → (N) Вариация
    ↓
Блюдо (1) ←→ (N) Группа модификаторов
    ↓
Группа модификаторов (1) → (N) Модификатор
    ↓
Модификатор (1) ←→ (N) Вариация (цены/вес в зависимости от вариации)
    ↓
Блюдо (N) ←→ (N) Тег
    ↓
Блюдо (N) ←→ (N) Город
Категория (N) ←→ (N) Город
```

---

### 1.4 Функциональные возможности

#### 1.4.1 Управление категориями

**Создание и редактирование**:

- Создать категорию с названием и описанием
- Задать порядок отображения (ручной или автоматический)
- Включить/отключить категорию глобально
- Включить/отключить категорию для конкретных городов
  **Отображение**:
- Категория отображается только если:
  - Она активна глобально
  - Она включена для текущего города
  - В ней есть хотя бы одно доступное блюдо

---

#### 1.4.2 Управление блюдами

**Создание и редактирование**:

- Базовая информация: название, описание, состав
- КБЖУ: указать на 100г и на всю порцию
- Вес с единицей измерения (г, кг, мл, л, шт)
- Загрузка одной фотографии
- Выбор категорий (множественный)
- Добавление тегов
- Настройка доступности:
  - Включить/отключить для городов (чекбоксы)
  - Включить/отключить для способов получения (доставка/самовывоз/зал)
- Настройка цен:
  - Базовая цена
  - Цены для разных городов
  - Цены для разных способов получения
    **Вариации**:
- Добавить вариации (если нужны размеры/порции)
- Для каждой вариации:
  - Название
  - Цена (может отличаться по городам и способам получения)
  - Вес с единицей измерения
  - КБЖУ (собственные)
    **Модификаторы**:
- Добавить глобальные группы модификаторов (выбрать из списка)
- При добавлении глобальной группы:
  - Можно отключить отдельные модификаторы
  - Цены остаются глобальными
- Создать уникальную группу модификаторов для блюда:
  - Указать тип (обязательная/необязательная)
  - Указать диапазон выбора (мин-макс)
  - Добавить модификаторы с ценами и весами
- Настроить цены модификаторов в зависимости от вариаций:
  - Вручную для каждой вариации
  - Автоматически (процент от первой вариации)
    **Стоп-лист**:
- Временно отключить блюдо для конкретного филиала
- Блюдо в стоп-листе отображается как недоступное (но видимое)

---

#### 1.4.3 Управление модификаторами

**Глобальные модификаторы**:

- Создать группу модификаторов (название, тип, диапазон выбора)
- Добавить модификаторы в группу:
  - Название
  - Цена
  - Вес (опционально)
  - Фотография
- Использовать в разных блюдах
  **Уникальные модификаторы**:
- Создать группу модификаторов для конкретного блюда
- Модификаторы доступны только для этого блюда

---

#### 1.4.4 Управление тегами

- Создать тег (название)
- Использовать теги для фильтрации в клиентской части
- Добавлять/удалять теги у блюд

---

#### 1.4.5 Стоп-лист

**Уровни стоп-листа**:

- Блюдо целиком (для филиала)
- Вариация блюда (для филиала)
- Модификатор (для филиала)
  **Поведение**:
- Позиция в стоп-листе отображается в меню как недоступная
- Клиент видит блюдо, но не может его заказать
- Информационное сообщение: "Временно недоступно"

---

### 1.5 Бизнес-логика и правила

#### 1.5.1 Правила доступности блюда

Блюдо **отображается** в меню, если:

1. ✅ Блюдо активно глобально
2. ✅ Блюдо включено для текущего города
3. ✅ Блюдо включено для текущего способа получения (доставка/самовывоз/зал)
4. ✅ Категория блюда активна и включена для города
5. ✅ Блюдо НЕ отключено в настройках города
   Блюдо **недоступно** для заказа (в стоп-листе), если:

- ❌ Блюдо в стоп-листе для текущего филиала
- ❌ Все вариации блюда в стоп-листе

---

#### 1.5.2 Правила обязательных модификаторов

Клиент **не может** добавить блюдо в корзину, если:

- Не выбран модификатор из обязательной группы
- Выбрано меньше модификаторов, чем минимум в группе
  Клиент **может** заказать блюдо, если:
- Все обязательные группы заполнены
- Количество выборов в каждой группе в допустимом диапазоне

---

#### 1.5.3 Правила расчета цены

**Итоговая цена блюда** = Базовая цена (или цена вариации) + Σ(цены модификаторов)
**Базовая цена** зависит от:

1. Города
2. Способа получения (доставка/самовывоз/зал)
3. Выбранной вариации (если есть)
   **Цена модификатора** зависит от:
4. Выбранной вариации (если настроено)
5. Базовой цены модификатора
   **Пример**:

```
Пицца "Маргарита" (Москва, Доставка)
├─ Вариация: 23 см - 599₽
├─ Модификатор: Моцарелла - +50₽ (для 23 см)
└─ Итого: 649₽
Пицца "Маргарита" (Москва, Доставка)
├─ Вариация: 28 см - 699₽
├─ Модификатор: Моцарелла - +80₽ (для 28 см)
└─ Итого: 779₽
```

---

#### 1.5.4 Правила отображения КБЖУ

**Если вариаций нет**:

- Показать КБЖУ основного блюда (на 100г и на порцию)
  **Если вариации есть**:
- По умолчанию: КБЖУ первой вариации
- При выборе вариации: КБЖУ выбранной вариации
- Формат: "Калории: 250 ккал (на 100г) / 625 ккал (на порцию 250г)"

---

#### 1.5.5 Правила сортировки

**Категории**:

- Если задан порядок вручную → сортировка по `sort_order`
- Если не задан → сортировка по `created_at` (сначала старые)
  **Блюда внутри категории**:
- Если задан порядок вручную → сортировка по `sort_order`
- Если не задан → сортировка по `created_at` (сначала старые)

---

## 2. Текущее состояние системы

### 2.1 Что уже реализовано

**Функциональность**:

- ✅ Базовое управление категориями (создание, редактирование, удаление)
- ✅ Базовое управление блюдами (создание, редактирование, удаление)
- ✅ Система вариаций блюд
- ✅ Система модификаторов с группами
- ✅ Привязка блюд к категориям
- ✅ Привязка модификаторов к блюдам через группы
- ✅ API для получения меню по городу
- ✅ Загрузка и оптимизация изображений
  **БД структура (существующие таблицы)**:

#### `menu_categories`

```sql
- id (INT, PK)
- name (VARCHAR)
- description (TEXT)
- is_active (BOOLEAN)
- created_at (TIMESTAMP)
- updated_at (TIMESTAMP)
```

#### `menu_items`

```sql
- id (INT, PK)
- category_id (INT, FK → menu_categories.id)
- name (VARCHAR)
- description (TEXT)
- composition (TEXT)
- weight (INT) -- только число, без единицы измерения
- price (DECIMAL)
- image_url (VARCHAR)
- is_active (BOOLEAN)
- created_at (TIMESTAMP)
- updated_at (TIMESTAMP)
```

#### `menu_item_variants`

```sql
- id (INT, PK)
- item_id (INT, FK → menu_items.id)
- name (VARCHAR)
- price (DECIMAL)
- weight (INT) -- только число, без единицы измерения
- created_at (TIMESTAMP)
- updated_at (TIMESTAMP)
```

#### `menu_modifier_groups`

```sql
- id (INT, PK)
- name (VARCHAR)
- is_required (BOOLEAN)
- min_selections (INT)
- max_selections (INT)
- created_at (TIMESTAMP)
- updated_at (TIMESTAMP)
```

#### `menu_modifiers`

```sql
- id (INT, PK)
- group_id (INT, FK → menu_modifier_groups.id)
- name (VARCHAR)
- price (DECIMAL)
- created_at (TIMESTAMP)
- updated_at (TIMESTAMP)
```

#### `menu_item_modifier_groups`

```sql
- id (INT, PK)
- item_id (INT, FK → menu_items.id)
- group_id (INT, FK → menu_modifier_groups.id)
- created_at (TIMESTAMP)
```

### 2.2 Что отсутствует (нужно добавить)

**Отсутствующий функционал**:

- ❌ Привязка категорий к городам (включение/отключение)
- ❌ Привязка блюд к городам (включение/отключение)
- ❌ Привязка блюд к нескольким категориям (сейчас только одна)
- ❌ Разные цены для разных способов получения (доставка/самовывоз/зал)
- ❌ Разные цены для разных городов
- ❌ Стоп-лист на уровне филиалов
- ❌ Теги для блюд
- ❌ Единицы измерения веса (г, кг, мл, л, шт)
- ❌ КБЖУ (калории, белки, жиры, углеводы)
- ❌ КБЖУ для вариаций (отдельные от основного блюда)
- ❌ Цены модификаторов в зависимости от выбранной вариации
- ❌ Фото для модификаторов
- ❌ Порядок сортировки категорий и блюд
- ❌ Вес для модификаторов
- ❌ Глобальные модификаторы (переиспользуемые)
- ❌ Включение/отключение отдельных модификаторов из глобальной группы

---

## 3. Изменения и доработки

### 3.1 Миграция базы данных

#### 3.1.1 Новые таблицы

##### `tags` (Таблица тегов)

```sql
CREATE TABLE tags (
  id INT AUTO_INCREMENT PRIMARY KEY,
  name VARCHAR(100) NOT NULL UNIQUE,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
);
```

##### `menu_item_tags` (Связь блюд с тегами)

```sql
CREATE TABLE menu_item_tags (
  id INT AUTO_INCREMENT PRIMARY KEY,
  item_id INT NOT NULL,
  tag_id INT NOT NULL,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  FOREIGN KEY (item_id) REFERENCES menu_items(id) ON DELETE CASCADE,
  FOREIGN KEY (tag_id) REFERENCES tags(id) ON DELETE CASCADE,
  UNIQUE KEY unique_item_tag (item_id, tag_id)
);
```

##### `menu_item_categories` (Связь блюд с несколькими категориями)

```sql
CREATE TABLE menu_item_categories (
  id INT AUTO_INCREMENT PRIMARY KEY,
  item_id INT NOT NULL,
  category_id INT NOT NULL,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  FOREIGN KEY (item_id) REFERENCES menu_items(id) ON DELETE CASCADE,
  FOREIGN KEY (category_id) REFERENCES menu_categories(id) ON DELETE CASCADE,
  UNIQUE KEY unique_item_category (item_id, category_id)
);
```

##### `menu_category_cities` (Привязка категорий к городам)

```sql
CREATE TABLE menu_category_cities (
  id INT AUTO_INCREMENT PRIMARY KEY,
  category_id INT NOT NULL,
  city_id INT NOT NULL,
  is_active BOOLEAN DEFAULT TRUE,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  FOREIGN KEY (category_id) REFERENCES menu_categories(id) ON DELETE CASCADE,
  FOREIGN KEY (city_id) REFERENCES cities(id) ON DELETE CASCADE,
  UNIQUE KEY unique_category_city (category_id, city_id)
);
```

##### `menu_item_cities` (Привязка блюд к городам)

```sql
CREATE TABLE menu_item_cities (
  id INT AUTO_INCREMENT PRIMARY KEY,
  item_id INT NOT NULL,
  city_id INT NOT NULL,
  is_active BOOLEAN DEFAULT TRUE,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  FOREIGN KEY (item_id) REFERENCES menu_items(id) ON DELETE CASCADE,
  FOREIGN KEY (city_id) REFERENCES cities(id) ON DELETE CASCADE,
  UNIQUE KEY unique_item_city (item_id, city_id)
);
```

##### `menu_item_prices` (Цены блюд по городам и способам получения)

```sql
CREATE TABLE menu_item_prices (
  id INT AUTO_INCREMENT PRIMARY KEY,
  item_id INT NOT NULL,
  city_id INT NULL, -- NULL = для всех городов
  fulfillment_type ENUM('delivery', 'pickup', 'dine_in') NOT NULL,
  price DECIMAL(10, 2) NOT NULL,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  FOREIGN KEY (item_id) REFERENCES menu_items(id) ON DELETE CASCADE,
  FOREIGN KEY (city_id) REFERENCES cities(id) ON DELETE CASCADE,
  UNIQUE KEY unique_item_city_fulfillment (item_id, city_id, fulfillment_type)
);
```

##### `menu_variant_prices` (Цены вариаций по городам и способам получения)

```sql
CREATE TABLE menu_variant_prices (
  id INT AUTO_INCREMENT PRIMARY KEY,
  variant_id INT NOT NULL,
  city_id INT NULL, -- NULL = для всех городов
  fulfillment_type ENUM('delivery', 'pickup', 'dine_in') NOT NULL,
  price DECIMAL(10, 2) NOT NULL,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  FOREIGN KEY (variant_id) REFERENCES menu_item_variants(id) ON DELETE CASCADE,
  FOREIGN KEY (city_id) REFERENCES cities(id) ON DELETE CASCADE,
  UNIQUE KEY unique_variant_city_fulfillment (variant_id, city_id, fulfillment_type)
);
```

##### `menu_modifier_variant_prices` (Цены модификаторов в зависимости от вариации)

```sql
CREATE TABLE menu_modifier_variant_prices (
  id INT AUTO_INCREMENT PRIMARY KEY,
  modifier_id INT NOT NULL,
  variant_id INT NOT NULL,
  price DECIMAL(10, 2) NOT NULL,
  weight DECIMAL(10, 2) NULL,
  weight_unit ENUM('g', 'kg', 'ml', 'l', 'pcs') NULL,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  FOREIGN KEY (modifier_id) REFERENCES menu_modifiers(id) ON DELETE CASCADE,
  FOREIGN KEY (variant_id) REFERENCES menu_item_variants(id) ON DELETE CASCADE,
  UNIQUE KEY unique_modifier_variant (modifier_id, variant_id)
);
```

##### `menu_stop_list` (Стоп-лист)

```sql
CREATE TABLE menu_stop_list (
  id INT AUTO_INCREMENT PRIMARY KEY,
  branch_id INT NOT NULL,
  entity_type ENUM('item', 'variant', 'modifier') NOT NULL,
  entity_id INT NOT NULL,
  reason TEXT NULL,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  created_by INT NULL, -- ID админа, который добавил в стоп-лист
  FOREIGN KEY (branch_id) REFERENCES branches(id) ON DELETE CASCADE,
  FOREIGN KEY (created_by) REFERENCES admin_users(id) ON DELETE SET NULL,
  UNIQUE KEY unique_branch_entity (branch_id, entity_type, entity_id)
);
```

##### `menu_item_disabled_modifiers` (Отключенные модификаторы в глобальных группах)

```sql
CREATE TABLE menu_item_disabled_modifiers (
  id INT AUTO_INCREMENT PRIMARY KEY,
  item_id INT NOT NULL,
  modifier_id INT NOT NULL,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  FOREIGN KEY (item_id) REFERENCES menu_items(id) ON DELETE CASCADE,
  FOREIGN KEY (modifier_id) REFERENCES menu_modifiers(id) ON DELETE CASCADE,
  UNIQUE KEY unique_item_modifier (item_id, modifier_id)
);
```

---

#### 3.1.2 Изменения существующих таблиц

##### `menu_categories` (добавить поля)

```sql
ALTER TABLE menu_categories
ADD COLUMN sort_order INT DEFAULT 0 AFTER description;
CREATE INDEX idx_categories_sort ON menu_categories(sort_order);
```

##### `menu_items` (добавить поля и изменить структуру)

```sql
-- Удалить старое поле category_id
ALTER TABLE menu_items
DROP FOREIGN KEY menu_items_ibfk_1;
ALTER TABLE menu_items
DROP COLUMN category_id;
-- Добавить новые поля
ALTER TABLE menu_items
ADD COLUMN weight_unit ENUM('g', 'kg', 'ml', 'l', 'pcs') DEFAULT 'g' AFTER weight,
ADD COLUMN calories_per_100g DECIMAL(10, 2) NULL AFTER weight_unit,
ADD COLUMN proteins_per_100g DECIMAL(10, 2) NULL AFTER calories_per_100g,
ADD COLUMN fats_per_100g DECIMAL(10, 2) NULL AFTER proteins_per_100g,
ADD COLUMN carbs_per_100g DECIMAL(10, 2) NULL AFTER fats_per_100g,
ADD COLUMN calories_per_serving DECIMAL(10, 2) NULL AFTER carbs_per_100g,
ADD COLUMN proteins_per_serving DECIMAL(10, 2) NULL AFTER calories_per_serving,
ADD COLUMN fats_per_serving DECIMAL(10, 2) NULL AFTER proteins_per_serving,
ADD COLUMN carbs_per_serving DECIMAL(10, 2) NULL AFTER fats_per_serving,
ADD COLUMN sort_order INT DEFAULT 0 AFTER carbs_per_serving;
CREATE INDEX idx_items_sort ON menu_items(sort_order);
```

##### `menu_item_variants` (добавить поля)

```sql
ALTER TABLE menu_item_variants
ADD COLUMN weight_unit ENUM('g', 'kg', 'ml', 'l', 'pcs') DEFAULT 'g' AFTER weight,
ADD COLUMN calories_per_100g DECIMAL(10, 2) NULL AFTER weight_unit,
ADD COLUMN proteins_per_100g DECIMAL(10, 2) NULL AFTER calories_per_100g,
ADD COLUMN fats_per_100g DECIMAL(10, 2) NULL AFTER proteins_per_100g,
ADD COLUMN carbs_per_100g DECIMAL(10, 2) NULL AFTER fats_per_100g,
ADD COLUMN calories_per_serving DECIMAL(10, 2) NULL AFTER carbs_per_100g,
ADD COLUMN proteins_per_serving DECIMAL(10, 2) NULL AFTER calories_per_serving,
ADD COLUMN fats_per_serving DECIMAL(10, 2) NULL AFTER proteins_per_serving,
ADD COLUMN carbs_per_serving DECIMAL(10, 2) NULL AFTER fats_per_serving;
```

##### `menu_modifiers` (добавить поля)

```sql
ALTER TABLE menu_modifiers
ADD COLUMN weight DECIMAL(10, 2) NULL AFTER price,
ADD COLUMN weight_unit ENUM('g', 'kg', 'ml', 'l', 'pcs') NULL AFTER weight,
ADD COLUMN image_url VARCHAR(255) NULL AFTER weight_unit;
```

##### `menu_modifier_groups` (добавить поле глобальности)

```sql
ALTER TABLE menu_modifier_groups
ADD COLUMN is_global BOOLEAN DEFAULT FALSE AFTER max_selections;
```

---

#### 3.1.3 Миграция существующих данных

**Шаг 1**: Перенести связи `menu_items.category_id` → `menu_item_categories`

```sql
INSERT INTO menu_item_categories (item_id, category_id)
SELECT id, category_id FROM menu_items WHERE category_id IS NOT NULL;
```

**Шаг 2**: Перенести цены блюд в `menu_item_prices`

```sql
INSERT INTO menu_item_prices (item_id, city_id, fulfillment_type, price)
SELECT id, NULL, 'delivery', price FROM menu_items WHERE price IS NOT NULL
UNION ALL
SELECT id, NULL, 'pickup', price FROM menu_items WHERE price IS NOT NULL
UNION ALL
SELECT id, NULL, 'dine_in', price FROM menu_items WHERE price IS NOT NULL;
```

**Шаг 3**: Перенести цены вариаций в `menu_variant_prices`

```sql
INSERT INTO menu_variant_prices (variant_id, city_id, fulfillment_type, price)
SELECT id, NULL, 'delivery', price FROM menu_item_variants WHERE price IS NOT NULL
UNION ALL
SELECT id, NULL, 'pickup', price FROM menu_item_variants WHERE price IS NOT NULL
UNION ALL
SELECT id, NULL, 'dine_in', price FROM menu_item_variants WHERE price IS NOT NULL;
```

**Шаг 4**: Создать привязку всех категорий ко всем городам

```sql
INSERT INTO menu_category_cities (category_id, city_id, is_active)
SELECT mc.id, c.id, TRUE
FROM menu_categories mc
CROSS JOIN cities c;
```

**Шаг 5**: Создать привязку всех блюд ко всем городам

```sql
INSERT INTO menu_item_cities (item_id, city_id, is_active)
SELECT mi.id, c.id, TRUE
FROM menu_items mi
CROSS JOIN cities c;
```

---

## 3.2 Изменения API

### 3.2.1 Новые эндпоинты

#### Теги

**GET** `/api/menu/admin/tags`

- Получить все теги
- Доступ: Admin, Manager, CEO
  **POST** `/api/menu/admin/tags`
- Создать тег
- Тело: `{ "name": "Без глютена" }`
  **PUT** `/api/menu/admin/tags/:id`
- Обновить тег
  **DELETE** `/api/menu/admin/tags/:id`
- Удалить тег

---

#### Привязка блюд к тегам

**POST** `/api/menu/admin/items/:itemId/tags`

- Добавить тег к блюду
- Тело: `{ "tag_id": 1 }`
  **DELETE** `/api/menu/admin/items/:itemId/tags/:tagId`
- Удалить тег у блюда

---

#### Привязка блюд к категориям (множественная)

**POST** `/api/menu/admin/items/:itemId/categories`

- Добавить блюдо в категорию
- Тело: `{ "category_id": 2 }`
  **DELETE** `/api/menu/admin/items/:itemId/categories/:categoryId`
- Удалить блюдо из категории

---

#### Управление ценами

**POST** `/api/menu/admin/items/:itemId/prices`

- Добавить/обновить цену блюда
- Тело: `{ "city_id": 1, "fulfillment_type": "delivery", "price": 599.00 }`
  **POST** `/api/menu/admin/variants/:variantId/prices`
- Добавить/обновить цену вариации
  **POST** `/api/menu/admin/modifiers/:modifierId/variant-prices`
- Настроить цены модификатора для вариаций
- Тело: `{ "variant_id": 1, "price": 50.00, "weight": 20, "weight_unit": "g" }`

---

#### Стоп-лист

**POST** `/api/menu/admin/stop-list`

- Добавить в стоп-лист
- Тело: `{ "branch_id": 1, "entity_type": "item", "entity_id": 5, "reason": "..." }`
  **DELETE** `/api/menu/admin/stop-list/:id`
- Удалить из стоп-листа
  **GET** `/api/menu/admin/stop-list?branch_id={id}`
- Получить стоп-лист филиала

---

#### Управление городами

**POST** `/api/menu/admin/categories/:categoryId/cities`

- Включить категорию для города
- Тело: `{ "city_id": 1, "is_active": true }`
  **PUT** `/api/menu/admin/categories/:categoryId/cities/:cityId`
- Изменить активность категории в городе
  **POST** `/api/menu/admin/items/:itemId/cities`
- Включить блюдо для города
  **PUT** `/api/menu/admin/items/:itemId/cities/:cityId`
- Изменить активность блюда в городе

---

#### Отключение модификаторов в глобальных группах

**POST** `/api/menu/admin/items/:itemId/disabled-modifiers`

- Отключить модификатор
- Тело: `{ "modifier_id": 3 }`
  **DELETE** `/api/menu/admin/items/:itemId/disabled-modifiers/:modifierId`
- Включить обратно модификатор

---

### 3.2.2 Изменения существующих эндпоинтов

#### GET `/api/menu?city_id={id}&branch_id={id}&fulfillment_type={type}`

**Новые параметры**:

- `branch_id` - ID филиала (для стоп-листа)
- `fulfillment_type` - способ получения (delivery/pickup/dine_in)
  **Новая логика**:

1. Фильтровать категории: активные, включенные для города, с доступными блюдами
2. Фильтровать блюда: активные, включенные для города и способа получения
3. Учитывать стоп-лист филиала
4. Вернуть цены для города и способа получения
5. Вернуть КБЖУ для блюд и вариаций
6. Вернуть теги
7. Вернуть цены модификаторов с учетом вариаций

---

#### POST `/api/menu/admin/items`

**Новые поля в теле запроса**:

```json
{
  "name": "Маргарита",
  "description": "...",
  "composition": "...",
  "weight": 450,
  "weight_unit": "g",
  "calories_per_100g": 250,
  "proteins_per_100g": 12,
  "fats_per_100g": 10,
  "carbs_per_100g": 30,
  "calories_per_serving": 1125,
  "proteins_per_serving": 54,
  "fats_per_serving": 45,
  "carbs_per_serving": 135,
  "sort_order": 1,
  "image_url": "/uploads/...",
  "is_active": true,
  "category_ids": [1, 5],
  "tag_ids": [2],
  "city_ids": [1, 2],
  "prices": [
    { "city_id": null, "fulfillment_type": "delivery", "price": 599 },
    { "city_id": null, "fulfillment_type": "pickup", "price": 549 },
    { "city_id": null, "fulfillment_type": "dine_in", "price": 499 }
  ]
}
```

---

#### PUT `/api/menu/admin/items/:id`

- Обновить с новыми полями (аналогично POST)

---

#### POST `/api/menu/admin/items/:itemId/variants`

**Новые поля**:

```json
{
  "name": "23 см",
  "weight": 350,
  "weight_unit": "g",
  "calories_per_100g": 250,
  "proteins_per_100g": 12,
  "fats_per_100g": 10,
  "carbs_per_100g": 30,
  "calories_per_serving": 875,
  "proteins_per_serving": 42,
  "fats_per_serving": 35,
  "carbs_per_serving": 105,
  "prices": [
    { "city_id": null, "fulfillment_type": "delivery", "price": 599 },
    { "city_id": null, "fulfillment_type": "pickup", "price": 549 }
  ]
}
```

---

#### POST `/api/menu/admin/modifier-groups`

## **Новое поле**: `is_global` (boolean)

#### POST `/api/menu/admin/modifier-groups/:groupId/modifiers`

## **Новые поля**: `weight`, `weight_unit`, `image_url`

### 3.3 Логика расчетов и валидаций

#### 3.3.1 Получение цены блюда

```sql
SELECT price FROM menu_item_prices
WHERE item_id = ?
  AND (city_id = ? OR city_id IS NULL)
  AND fulfillment_type = ?
ORDER BY city_id DESC NULLS LAST
LIMIT 1;
```

#### 3.3.2 Получение цены модификатора

```sql
SELECT
  COALESCE(mvp.price, m.price) as price,
  COALESCE(mvp.weight, m.weight) as weight,
  COALESCE(mvp.weight_unit, m.weight_unit) as weight_unit
FROM menu_modifiers m
LEFT JOIN menu_modifier_variant_prices mvp
  ON mvp.modifier_id = m.id AND mvp.variant_id = ?
WHERE m.id = ?;
```

#### 3.3.3 Проверка доступности блюда

```sql
SELECT COUNT(*) > 0 FROM menu_items mi
JOIN menu_item_cities mic ON mic.item_id = mi.id
JOIN menu_category_cities mcc ON mcc.category_id IN (
  SELECT category_id FROM menu_item_categories WHERE item_id = mi.id
)
LEFT JOIN menu_stop_list msl ON msl.entity_type = 'item'
  AND msl.entity_id = mi.id
  AND msl.branch_id = ?
WHERE mi.id = ?
  AND mi.is_active = TRUE
  AND mic.city_id = ? AND mic.is_active = TRUE
  AND mcc.city_id = ? AND mcc.is_active = TRUE
  AND msl.id IS NULL;
```

#### 3.3.4 Валидация заказа

```javascript
function validateOrderItem(item, selectedModifiers) {
  const requiredGroups = item.modifier_groups.filter((g) => g.is_required);
  for (const group of requiredGroups) {
    const selectedFromGroup = selectedModifiers.filter((m) => group.modifiers.some((gm) => gm.id === m.id));
    if (selectedFromGroup.length < group.min_selections) {
      throw new Error(`Нужно выбрать минимум ${group.min_selections} из "${group.name}"`);
    }
    if (selectedFromGroup.length > group.max_selections) {
      throw new Error(`Максимум ${group.max_selections} из "${group.name}"`);
    }
  }
  return true;
}
```

---

## 4. UI/UX спецификация

### 4.1 Админ-панель

#### 4.1.1 Текущие экраны (что уже есть)

**Существующие страницы**:

- `/admin/menu/categories` - список категорий
- `/admin/menu/items` - список блюд
- `/admin/menu/modifier-groups` - список групп модификаторов
  **Текущий функционал**: базовые CRUD операции

---

#### 4.1.2 Новые экраны

##### Управление тегами (`/admin/menu/tags`)

```
┌─────────────────────────────────────────┐
│  Теги                          [+ Создать]│
├─────────────────────────────────────────┤
│  ID │ Название      │ Действия          │
│  1  │ Острое        │ [Изм] [Удал]      │
│  2  │ Веган         │ [Изм] [Удал]      │
└─────────────────────────────────────────┘
```

##### Стоп-лист (`/admin/menu/stop-list`)

```
┌─────────────────────────────────────────┐
│  Стоп-лист                   [+ Добавить]│
│  Филиал: [Москва - Центр ▼]            │
├─────────────────────────────────────────┤
│  Тип   │ Название    │ Причина │ Действ │
│  Блюдо │ Маргарита   │ Нет ... │ [Убр] │
└─────────────────────────────────────────┘
```

---

#### 4.1.3 Изменения существующих экранов

##### Форма категории

**Новые поля**:

- Порядок (sort_order)
- Доступность по городам (чекбоксы)

```
Название*: [_______]
Описание:  [_______]
Порядок:   [___] (0 = авто)
☑ Активна
Доступность по городам:
☑ Москва
☑ СПб
☐ Казань
[Отмена] [Сохранить]
```

---

##### Форма блюда (с табами)

**Таб 1: Основная информация**

```
Название*:     [___]
Описание:      [___]
Состав:        [___]
Фото:          [📷 Загрузить]
Вес*:          [___] [грамм ▼]
КБЖУ на 100г:
  Калории:     [___] ккал
  Белки:       [___] г
  Жиры:        [___] г
  Углеводы:    [___] г
КБЖУ на порцию:
  Калории:     [___] ккал
  Белки:       [___] г
  Жиры:        [___] г
  Углеводы:    [___] г
Порядок:       [___]
☑ Активно
Категории*:
☑ Пицца
☐ Суши
☑ Вегетарианское
```

**Таб 2: Вариации**

```
Вариации блюда          [+ Добавить]
1. "23 см"              [Изменить]
   Вес: 350 г           [Удалить]
   КБЖУ: 250/12/10/30 (на 100г)
   КБЖУ: 875/42/35/105 (на порцию)
2. "28 см"              [Изменить]
   Вес: 550 г           [Удалить]
   КБЖУ: 250/12/10/30 (на 100г)
   КБЖУ: 1375/66/55/165 (на порцию)
```

**Таб 3: Модификаторы**

```
[+ Добавить глобальную группу]
[+ Создать уникальную группу]
1. Группа "Основа" (глобальная)
   Обязательная, выбор: 1
   ☑ Традиционное тесто (0₽)
   ☑ Тонкое тесто (0₽)
   ☐ Сырный борт (+100₽) [отключен]
                        [Отвязать]
2. Группа "Топпинги" (уникальная)
   Необязательная, выбор: 0-5
   • Моцарелла (50₽/80₽) [Изм]
     Цены зависят от вариации
   • Пармезан (70₽) [Изм]
                    [Удал]
```

**Модальное окно настройки цен модификатора**:

```
┌─────────────────────────────────────┐
│ Настройка цен для "Моцарелла"       │
├─────────────────────────────────────┤
│ Режим расчета:                      │
│ ○ Одинаковая цена                   │
│ ● Разные цены (вручную)             │
│ ○ Автоматический (% от первой)      │
│                                     │
│ Вариация "23 см":                   │
│   Цена:  [50.00] ₽  Вес: [20] г    │
│                                     │
│ Вариация "28 см":                   │
│   Цена:  [80.00] ₽  Вес: [30] г    │
│                                     │
│      [Отмена]  [Сохранить]          │
└─────────────────────────────────────┘
```

**Таб 4: Цены**

```
☐ Использовать разные цены по городам
Базовые цены (для всех городов):
Блюдо (без вариаций):
  Доставка:   [599.00] ₽
  Самовывоз:  [549.00] ₽
  В зале:     [499.00] ₽
Вариация "23 см":
  Доставка:   [599.00] ₽
  Самовывоз:  [549.00] ₽
  В зале:     [499.00] ₽
Вариация "28 см":
  Доставка:   [699.00] ₽
  Самовывоз:  [649.00] ₽
  В зале:     [599.00] ₽
```

**При включении "Разные цены по городам"**:

```
☑ Использовать разные цены по городам
Город: [Москва ▼]
Цены для Москвы:
  Доставка:   [699.00] ₽ (+100₽)
  Самовывоз:  [649.00] ₽ (+100₽)
  В зале:     [599.00] ₽ (+100₽)
```

**Таб 5: Города**

```
Доступность по городам:
☑ Москва
☑ СПб
☐ Казань
Доступность по способам:
☑ Доставка
☑ Самовывоз
☐ В зале
```

**Таб 6: Теги**

```
☐ Острое
☑ Веган
☐ Без глютена
☑ Новинка
[+ Создать тег]
```

---

##### Форма группы модификаторов

```
┌─────────────────────────────────────┐
│ Редактирование группы               │
├─────────────────────────────────────┤
│ Название*: [_______]                │
│                                     │
│ ☑ Глобальная (переиспользуемая)     │
│                                     │
│ Тип:                                │
│ ● Обязательная                      │
│ ○ Необязательная                    │
│                                     │
│ Диапазон:                           │
│   Минимум: [1]  Максимум: [1]       │
│                                     │
│ Модификаторы:        [+ Добавить]   │
│                                     │
│ 1. Кетчуп           [Изм] [Удал]    │
│    0₽, [📷]                         │
│                                     │
│ 2. Майонез          [Изм] [Удал]    │
│    0₽, [📷]                         │
│                                     │
│      [Отмена]  [Сохранить]          │
└─────────────────────────────────────┘
```

**Форма модификатора**:

```
┌─────────────────────────────────────┐
│ Добавить модификатор                │
├─────────────────────────────────────┤
│ Название*: [_______]                │
│ Цена*:     [____] ₽                 │
│ Вес:       [____] [грамм ▼]         │
│ Фото:      [📷 Загрузить]           │
│                                     │
│      [Отмена]  [Добавить]           │
└─────────────────────────────────────┘
```

---

### 4.2 Клиентская часть (Telegram Mini App)

#### 4.2.1 Текущее отображение

**Существующие экраны**:

- Список категорий (горизонтальная прокрутка)
- Список блюд в категории
- Модальное окно блюда

---

#### 4.2.2 Новое поведение

##### Список блюд

```
┌─────────────────────────────────────┐
│ [🌶️ Острое] [🌱 Веган] [Все ▼]     │
├─────────────────────────────────────┤
│ ┌─────────────────────────────────┐ │
│ │ [Фото]  Маргарита         599₽  │ │
│ │         Классическая пицца      │ │
│ │         🌱 Веган                │ │
│ │                   [В корзину]   │ │
│ └─────────────────────────────────┘ │
│                                     │
│ ┌─────────────────────────────────┐ │
│ │ [Фото]  Пепперони      Недост.  │ │
│ │         С салями                │ │
│ │         ⚠️ Временно недоступно   │ │
│ └─────────────────────────────────┘ │
└─────────────────────────────────────┘
```

**Изменения**:

- Фильтр по тегам
- Значки тегов на карточках
- Индикация стоп-листа
- Цена зависит от города и способа получения

---

##### Модальное окно блюда

```
┌─────────────────────────────────────┐
│ [×]                                 │
│ [Фото блюда]                        │
│                                     │
│ Маргарита                     599₽  │
│ 🌱 Веган                            │
│                                     │
│ Классическая пицца с томатами       │
│                                     │
│ Состав: тесто, томаты, моцарелла   │
│                                     │
│ ────────────────────────────────    │
│                                     │
│ Выберите размер: *                  │
│ ○ 23 см (350 г)  599₽              │
│ ● 28 см (550 г)  699₽              │
│ ○ 33 см (750 г)  899₽              │
│                                     │
│ КБЖУ на 100г / на порцию:           │
│ 250 ккал (1375) • Б: 12г (66г)     │
│ Ж: 10г (55г) • У: 30г (165г)       │
│                                     │
│ ────────────────────────────────    │
│                                     │
│ Основа: *                           │
│ ● Традиционное  0₽                  │
│ ○ Тонкое        0₽                  │
│                                     │
│ ────────────────────────────────    │
│                                     │
│ Топпинги (0-5):                     │
│ ☐ Моцарелла     80₽ [📷]           │
│ ☐ Пармезан      70₽ [📷]           │
│ ☐ Грибы      Недоступно [📷]       │
│                                     │
│ ────────────────────────────────    │
│                                     │
│ Количество:  [-] 1 [+]              │
│                                     │
│ [В корзину за 779₽]                 │
└─────────────────────────────────────┘
```

**Изменения**:

- Теги под названием
- Показ состава
- КБЖУ обновляется при выборе вариации
- Цены модификаторов зависят от вариации
- Фото модификаторов
- Индикация недоступных модификаторов
- Валидация обязательных групп

---

## 5. План миграции

### 5.1 Последовательность

**Этап 1: БД**

1. Создать новые таблицы
2. Добавить поля в существующие таблицы
3. Миграция данных
4. Тестирование
   **Этап 2: Backend**
5. Новые endpoints
6. Обновить существующие
7. Логика расчетов
8. Тесты
   **Этап 3: Админка**
9. Теги
10. Стоп-лист
11. Формы категорий/блюд
12. Формы модификаторов
    **Этап 4: Клиент**
13. Фильтр тегов
14. Обновить карточки блюд
15. Модальное окно
16. Валидация
    **Этап 5: Тестирование**
    **Этап 6: Развертывание**

---

### 5.2 Обратная совместимость

**Критично**:

- Старые API работают
- Данные не теряются
- Fallback на дефолтные значения
- Клиент работает со старым backend
  **Примеры**:
- `GET /api/menu?city_id=1` (без новых параметров) → работает
- `menu_items.price` → используется как fallback
- `menu_items.category_id` → удаляется после миграции

---

### 5.3 Стратегия отката

**Если ошибка**:

1. Восстановить БД из бэкапа
2. Откатить backend (git + pm2)
3. Откатить frontend
4. Исправить и повторить

---

## 6. Доработки фазы 2

### 6.1 Массовые операции

```
Выбрано: 12 блюд
Действие: [Изменить цены ▼]
Изменить на:
○ Фиксированную (+100₽)
● Процент (+10%)
Применить к:
☑ Доставка
☑ Самовывоз
Города:
☑ Все города
[Отмена] [Применить]
```

---

### 6.2 История изменений

```
История: "Маргарита"
15.01.2026 14:30 | Иван (Admin)
Цена: 549₽ → 599₽
                [Восстановить]
10.01.2026 09:15 | Мария (CEO)
Добавлен тег "Веган"
                [Восстановить]
```

---

### 6.3 Дополнительно

- Галерея фото
- Фото для вариаций
- Время продажи
- Рекомендации
- Сезонные блюда
- Импорт/экспорт
- Дублирование

---

## 7. Примеры API запросов

### Получение меню

**Запрос**:

```
GET /api/menu?city_id=1&branch_id=2&fulfillment_type=delivery
```

## **Ответ**: см. часть 1

### Создание блюда

**Запрос**:

```json
POST /api/menu/admin/items
{
  "name": "Маргарита",
  "weight": 450,
  "weight_unit": "g",
  "calories_per_100g": 250,
  "category_ids": [1, 5],
  "tag_ids": [2],
  "prices": [
    { "fulfillment_type": "delivery", "price": 599 }
  ]
}
```

---

### Стоп-лист

```json
POST /api/menu/admin/stop-list
{
  "branch_id": 2,
  "entity_type": "item",
  "entity_id": 10,
  "reason": "Закончилась моцарелла"
}
```

---

## 8. Заключение

**Ключевые достижения**:

- Гибкое мультигородное меню
- Дифференцированное ценообразование
- Стоп-лист
- Теги
- КБЖУ
- Глобальные модификаторы
- Обратная совместимость

---

## 9. ОТЧЕТ ПО АНАЛИЗУ ТЕКУЩЕГО СОСТОЯНИЯ ПРОЕКТА

**Дата анализа:** 19 января 2026 г.  
**Анализировал:** AI Assistant  
**Версия проекта:** 2.0

---

### 9.1 Текущая архитектура системы

#### 9.1.1 Backend (Node.js + Express)

**Реализованные API endpoints:**
**Публичные эндпоинты:**

- `GET /api/menu` - получение полного меню по городу (с кешированием Redis)
- `GET /api/menu/categories` - список активных категорий
- `GET /api/menu/categories/:id` - получение категории по ID
- `GET /api/menu/categories/:categoryId/items` - позиции категории
- `GET /api/menu/items/:id` - детали позиции с вариантами и модификаторами
- `GET /api/menu/items/:itemId/modifiers` - модификаторы позиции (старая система)
- `GET /api/menu/items/:itemId/variants` - варианты позиции
- `GET /api/menu/modifier-groups` - список групп модификаторов
- `GET /api/menu/modifier-groups/:id` - группа модификаторов с модификаторами
- `GET /api/menu/modifier-groups/:groupId/modifiers` - модификаторы группы
  **Админские эндпоинты:**
  _Категории:_
- `GET /api/menu/admin/categories` - все категории города
- `POST /api/menu/admin/categories` - создание категории
- `PUT /api/menu/admin/categories/:id` - обновление категории
- `DELETE /api/menu/admin/categories/:id` - удаление категории
  _Позиции меню:_
- `GET /api/menu/admin/categories/:categoryId/items` - позиции категории
- `POST /api/menu/admin/items` - создание позиции
- `PUT /api/menu/admin/items/:id` - обновление позиции
- `DELETE /api/menu/admin/items/:id` - удаление позиции
- `GET /api/menu/admin/items/:itemId/modifiers` - модификаторы позиции
- `GET /api/menu/admin/items/:itemId/variants` - варианты позиции
- `POST /api/menu/admin/items/:itemId/variants` - создание варианта
- `PUT /api/menu/admin/variants/:id` - обновление варианта
- `DELETE /api/menu/admin/variants/:id` - удаление варианта
  _Группы модификаторов:_
- `GET /api/menu/admin/modifier-groups` - все группы
- `POST /api/menu/admin/modifier-groups` - создание группы
- `PUT /api/menu/admin/modifier-groups/:id` - обновление группы
- `DELETE /api/menu/admin/modifier-groups/:id` - удаление группы
- `POST /api/menu/admin/items/:itemId/modifier-groups` - привязка группы к позиции
- `GET /api/menu/admin/items/:itemId/modifier-groups` - группы позиции
- `DELETE /api/menu/admin/items/:itemId/modifier-groups/:groupId` - отвязка группы
  _Модификаторы:_
- `POST /api/menu/admin/modifier-groups/:groupId/modifiers` - создание модификатора
- `PUT /api/menu/admin/modifiers/:id` - обновление модификатора
- `DELETE /api/menu/admin/modifiers/:id` - удаление модификатора
  **Особенности:**
- Кеширование меню в Redis с TTL 300 секунд
- Автоматическая инвалидация кеша при изменениях
- Контроль доступа по ролям (admin, manager, ceo)
- Менеджеры ограничены своими городами

---

#### 9.1.2 База данных (MySQL 8.0)

**Текущая структура:**
**Существующие таблицы:**

1. `menu_categories` - категории меню (привязаны к городам)
   - Поля: id, city_id, name, description, image_url, sort_order, is_active
2. `menu_items` - позиции меню
   - Поля: id, category_id, name, description, price, image_url, weight, weight_value, weight_unit, calories, sort_order, is_active
3. `item_variants` - варианты позиций (размеры)
   - Поля: id, item_id, name, price, weight_value, weight_unit, sort_order, is_active
4. `modifier_groups` - группы модификаторов
   - Поля: id, name, type (single/multiple), is_required, is_active
5. `modifiers` - модификаторы в группах
   - Поля: id, group_id, name, price, sort_order, is_active
6. `item_modifier_groups` - связь позиций и групп модификаторов
   - Поля: id, item_id, modifier_group_id
7. `menu_modifiers` - старая система модификаторов (для совместимости)
   - Поля: id, item_id, name, price, is_required, is_active
     **Миграции:**

- Добавлены поля weight_value и weight_unit для позиций и вариантов
- Создана новая система вариантов и групп модификаторов
- Обеспечена обратная совместимость со старой системой

---

#### 9.1.3 Admin Panel (Vue 3 + Vite)

**Реализованные экраны:**

1. **MenuCategories.vue** - управление категориями
   - Выбор города
   - CRUD операции для категорий
   - Поля: название, описание, порядок, статус
   - ✅ Реализовано: базовый CRUD, сортировка
   - ❌ Отсутствует: управление доступностью по городам
2. **MenuItems.vue** - управление позициями меню
   - Фильтрация по городу и категории
   - CRUD операции для позиций
   - Загрузка изображений
   - Управление вариантами (добавление/удаление)
   - Привязка групп модификаторов (чекбоксы)
   - ✅ Реализовано: базовый CRUD, варианты, привязка групп
   - ❌ Отсутствует:
     - Множественные категории
     - Теги
     - КБЖУ
     - Цены по городам/способам получения
     - Отключение отдельных модификаторов из группы
3. **MenuModifiers.vue** - управление группами модификаторов
   - CRUD для групп модификаторов
   - CRUD для модификаторов внутри групп
   - Типы: single/multiple
   - Обязательность группы
   - ✅ Реализовано: полный CRUD, типы групп
   - ❌ Отсутствует: - Поле is_global (глобальные группы) - Диапазон выбора (min/max) - Фото для модификаторов - Вес модификаторов - Цены модификаторов по вариациям
     **UI/UX:**

- Использует современный дизайн с компонентами UI (Card, Button, Table, Modal)
- Адаптивный интерфейс
- Валидация форм
- Сообщения об ошибках

---

#### 9.1.4 Telegram Mini App (Vue 3 + Vite)

**Реализованные экраны:**

1. **Home.vue** - главная страница с меню
   - Отображение активных заказов
   - Переключение доставка/самовывоз
   - Выбор адреса/филиала
   - Горизонтальная навигация по категориям (sticky)
   - Список блюд с изображениями
   - Добавление в корзину
   - Счетчики количества в корзине
   - Плавающая кнопка корзины
   - ✅ Реализовано: полная навигация, корзина, UI
   - ❌ Отсутствует:
     - Фильтр по тегам
     - Индикация стоп-листа
2. **ItemDetail.vue** - детальная страница позиции
   - Изображение товара
   - Название и описание
   - Вес позиции
   - Выбор варианта (кнопки с ценами)
   - Обязательные группы модификаторов
   - Опциональные группы модификаторов
   - Radio/checkbox для модификаторов
   - Валидация обязательных групп
   - Кнопка добавления в корзину с ценой
   - Счетчик количества для уже добавленных
   - ✅ Реализовано: полный функционал выбора
   - ❌ Отсутствует: - Теги блюда - КБЖУ (на 100г и на порцию) - Состав блюда - Фото модификаторов - Индикация недоступных модификаторов - Цены модификаторов в зависимости от вариации
     **Особенности:**

- Использует haptic feedback для нативного ощущения
- Обработка клавиатуры (скрытие кнопок)
- WebSocket для real-time обновлений
- Intersection Observer для активной категории
- Синхронизация состояния через Pinia stores

---

### 9.2 Сравнение с требованиями документации

#### 9.2.1 ЧТО РЕАЛИЗОВАНО ✅

**База данных:**

- ✅ Таблица категорий с привязкой к городу
- ✅ Таблица позиций меню
- ✅ Таблица вариантов позиций
- ✅ Таблица групп модификаторов
- ✅ Таблица модификаторов
- ✅ Связь позиций и групп модификаторов
- ✅ Поля weight_value и weight_unit
- ✅ Поле sort_order для категорий и позиций
  **Backend API:**
- ✅ Получение меню по городу
- ✅ CRUD категорий
- ✅ CRUD позиций
- ✅ CRUD вариантов
- ✅ CRUD групп модификаторов
- ✅ CRUD модификаторов
- ✅ Привязка/отвязка групп к позициям
- ✅ Кеширование меню в Redis
- ✅ Контроль доступа по ролям
  **Admin Panel:**
- ✅ Управление категориями
- ✅ Управление позициями
- ✅ Управление вариантами
- ✅ Управление группами модификаторов
- ✅ Загрузка изображений
- ✅ Сортировка
  **Client App:**
- ✅ Отображение меню по категориям
- ✅ Выбор вариантов
- ✅ Выбор модификаторов
- ✅ Валидация обязательных групп
- ✅ Корзина с количеством

---

#### 9.2.2 ЧТО ОТСУТСТВУЕТ ❌

**База данных:**

- ❌ Таблица `tags` - теги для блюд
- ❌ Таблица `menu_item_tags` - связь блюд с тегами
- ❌ Таблица `menu_item_categories` - множественные категории для блюда
- ❌ Таблица `menu_category_cities` - управление доступностью категорий по городам
- ❌ Таблица `menu_item_cities` - управление доступностью блюд по городам
- ❌ Таблица `menu_item_prices` - цены по городам и способам получения
- ❌ Таблица `menu_variant_prices` - цены вариантов по городам
- ❌ Таблица `menu_modifier_variant_prices` - цены модификаторов по вариациям
- ❌ Таблица `menu_stop_list` - стоп-лист по филиалам
- ❌ Таблица `menu_item_disabled_modifiers` - отключенные модификаторы
- ❌ Поля КБЖУ в `menu_items` (calories_per_100g, proteins_per_100g, fats_per_100g, carbs_per_100g)
- ❌ Поля КБЖУ в `item_variants`
- ❌ Поле `composition` в `menu_items` (состав блюда)
- ❌ Поля `weight`, `weight_unit`, `image_url` в `modifiers`
- ❌ Поле `is_global` в `modifier_groups`
- ❌ Поля `min_selections`, `max_selections` в `modifier_groups`
  **Backend API:**
- ❌ API для тегов (CRUD)
- ❌ API привязки тегов к блюдам
- ❌ API для множественных категорий
- ❌ API управления доступностью категорий по городам
- ❌ API управления доступностью блюд по городам
- ❌ API управления ценами (по городам и способам получения)
- ❌ API стоп-листа
- ❌ API отключения модификаторов в глобальных группах
- ❌ Параметры `branch_id` и `fulfillment_type` в GET /api/menu
- ❌ Фильтрация по стоп-листу
- ❌ Возврат цен с учетом города и способа получения
  **Admin Panel:**
- ❌ Экран управления тегами
- ❌ Экран стоп-листа
- ❌ Табы в форме позиции:
  - ❌ Таб "Основная информация" с КБЖУ
  - ❌ Таб "Вариации" с КБЖУ для вариаций
  - ❌ Таб "Модификаторы" с настройкой цен по вариациям
  - ❌ Таб "Цены" с ценами по городам/способам
  - ❌ Таб "Города" с доступностью
  - ❌ Таб "Теги"
- ❌ Множественный выбор категорий
- ❌ Управление доступностью категорий по городам
- ❌ Настройка глобальности групп модификаторов
- ❌ Диапазон выбора (min/max) для групп
- ❌ Фото для модификаторов
- ❌ Вес для модификаторов
- ❌ Цены модификаторов в зависимости от вариации
- ❌ Отключение отдельных модификаторов из глобальной группы
  **Client App:**
- ❌ Фильтр по тегам
- ❌ Отображение тегов на карточках
- ❌ Индикация стоп-листа
- ❌ КБЖУ на странице товара
- ❌ Состав блюда
- ❌ Фото модификаторов
- ❌ Цены модификаторов меняются при выборе вариации
- ❌ Индикация недоступных модификаторов

---

### 9.3 Критические замечания

#### 9.3.1 Архитектурные проблемы

1. **Привязка категорий к городам** - сейчас категории жестко привязаны к одному городу (`city_id` в `menu_categories`). По документации категории должны быть глобальными с возможностью включения/отключения для конкретных городов.
2. **Привязка позиций к категориям** - сейчас позиция может быть только в одной категории (`category_id` в `menu_items`). По документации позиция может быть в нескольких категориях одновременно.
3. **Система цен** - сейчас одна цена для позиции/варианта. По документации должны быть разные цены для разных городов и способов получения (доставка/самовывоз/зал).
4. **Отсутствие стоп-листа** - нет механизма временного отключения позиций/вариантов/модификаторов на уровне филиалов.
5. **Модификаторы без гибкости** - нет цен в зависимости от вариации, нет фото, нет веса.
6. **Группы модификаторов** - нет концепции глобальных групп, нет диапазона выбора (min/max).

---

#### 9.3.2 Функциональные ограничения

1. **Теги не реализованы** - нет фильтрации и поиска по тегам
2. **КБЖУ отсутствует** - нет информации о пищевой ценности
3. **Состав блюда не отображается** - клиент не видит ингредиенты
4. **Нет управления доступностью** - нельзя включать/отключать для городов
5. **Нет массовых операций** - каждое изменение по отдельности
6. **Нет истории изменений** - нельзя откатить изменения

---

### 9.4 Выводы

**Текущее состояние:** Проект имеет **солидную базу** с работающей системой меню, вариантов и модификаторов. Реализована вся инфраструктура (backend, admin, client), есть кеширование, контроль доступа.
**Уровень готовности:** ~40% от полного функционала по документации
**Что работает хорошо:**

- ✅ Базовая система меню
- ✅ Варианты позиций
- ✅ Группы модификаторов
- ✅ UI/UX дизайн
- ✅ Кеширование
- ✅ Безопасность
  **Что требует доработки:**
- ❌ Гибкая система ценообразования
- ❌ Мультигородность и мультикатегорийность
- ❌ Теги и фильтрация
- ❌ КБЖУ и состав
- ❌ Стоп-лист
- ❌ Гибкие модификаторы
  **Рекомендации:**

1. Приоритет - миграция БД (новые таблицы)
2. Затем - backend API
3. Затем - admin panel
4. В последнюю очередь - client app

---

## 10. ROADMAP 2.0 - ПЛАН РЕАЛИЗАЦИИ

**Цель:** Реализовать полный функционал системы управления меню согласно документации
**Срок:** 6-8 недель (при работе 1 разработчика full-time)

---

### Этап 1: Миграция базы данных (1-2 недели)

#### Неделя 1: Создание новых таблиц

**Приоритет: КРИТИЧЕСКИЙ**
**Задачи:**
1.1. **Создать таблицу тегов**

```sql
CREATE TABLE tags (
  id INT AUTO_INCREMENT PRIMARY KEY,
  name VARCHAR(100) NOT NULL UNIQUE,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
);
CREATE TABLE menu_item_tags (
  id INT AUTO_INCREMENT PRIMARY KEY,
  item_id INT NOT NULL,
  tag_id INT NOT NULL,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  FOREIGN KEY (item_id) REFERENCES menu_items(id) ON DELETE CASCADE,
  FOREIGN KEY (tag_id) REFERENCES tags(id) ON DELETE CASCADE,
  UNIQUE KEY unique_item_tag (item_id, tag_id)
);
```

1.2. **Создать таблицу множественных категорий**

```sql
CREATE TABLE menu_item_categories (
  id INT AUTO_INCREMENT PRIMARY KEY,
  item_id INT NOT NULL,
  category_id INT NOT NULL,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  FOREIGN KEY (item_id) REFERENCES menu_items(id) ON DELETE CASCADE,
  FOREIGN KEY (category_id) REFERENCES menu_categories(id) ON DELETE CASCADE,
  UNIQUE KEY unique_item_category (item_id, category_id)
);
```

1.3. **Создать таблицы управления доступностью**

```sql
CREATE TABLE menu_category_cities (
  id INT AUTO_INCREMENT PRIMARY KEY,
  category_id INT NOT NULL,
  city_id INT NOT NULL,
  is_active BOOLEAN DEFAULT TRUE,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  FOREIGN KEY (category_id) REFERENCES menu_categories(id) ON DELETE CASCADE,
  FOREIGN KEY (city_id) REFERENCES cities(id) ON DELETE CASCADE,
  UNIQUE KEY unique_category_city (category_id, city_id)
);
CREATE TABLE menu_item_cities (
  id INT AUTO_INCREMENT PRIMARY KEY,
  item_id INT NOT NULL,
  city_id INT NOT NULL,
  is_active BOOLEAN DEFAULT TRUE,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  FOREIGN KEY (item_id) REFERENCES menu_items(id) ON DELETE CASCADE,
  FOREIGN KEY (city_id) REFERENCES cities(id) ON DELETE CASCADE,
  UNIQUE KEY unique_item_city (item_id, city_id)
);
```

1.4. **Создать таблицы цен**

```sql
CREATE TABLE menu_item_prices (
  id INT AUTO_INCREMENT PRIMARY KEY,
  item_id INT NOT NULL,
  city_id INT NULL,
  fulfillment_type ENUM('delivery', 'pickup', 'dine_in') NOT NULL,
  price DECIMAL(10, 2) NOT NULL,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  FOREIGN KEY (item_id) REFERENCES menu_items(id) ON DELETE CASCADE,
  FOREIGN KEY (city_id) REFERENCES cities(id) ON DELETE CASCADE,
  UNIQUE KEY unique_item_city_fulfillment (item_id, city_id, fulfillment_type)
);
CREATE TABLE menu_variant_prices (
  id INT AUTO_INCREMENT PRIMARY KEY,
  variant_id INT NOT NULL,
  city_id INT NULL,
  fulfillment_type ENUM('delivery', 'pickup', 'dine_in') NOT NULL,
  price DECIMAL(10, 2) NOT NULL,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  FOREIGN KEY (variant_id) REFERENCES item_variants(id) ON DELETE CASCADE,
  FOREIGN KEY (city_id) REFERENCES cities(id) ON DELETE CASCADE,
  UNIQUE KEY unique_variant_city_fulfillment (variant_id, city_id, fulfillment_type)
);
CREATE TABLE menu_modifier_variant_prices (
  id INT AUTO_INCREMENT PRIMARY KEY,
  modifier_id INT NOT NULL,
  variant_id INT NOT NULL,
  price DECIMAL(10, 2) NOT NULL,
  weight DECIMAL(10, 2) NULL,
  weight_unit ENUM('g', 'kg', 'ml', 'l', 'pcs') NULL,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  FOREIGN KEY (modifier_id) REFERENCES modifiers(id) ON DELETE CASCADE,
  FOREIGN KEY (variant_id) REFERENCES item_variants(id) ON DELETE CASCADE,
  UNIQUE KEY unique_modifier_variant (modifier_id, variant_id)
);
```

1.5. **Создать таблицу стоп-листа**

```sql
CREATE TABLE menu_stop_list (
  id INT AUTO_INCREMENT PRIMARY KEY,
  branch_id INT NOT NULL,
  entity_type ENUM('item', 'variant', 'modifier') NOT NULL,
  entity_id INT NOT NULL,
  reason TEXT NULL,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  created_by INT NULL,
  FOREIGN KEY (branch_id) REFERENCES branches(id) ON DELETE CASCADE,
  FOREIGN KEY (created_by) REFERENCES admin_users(id) ON DELETE SET NULL,
  UNIQUE KEY unique_branch_entity (branch_id, entity_type, entity_id)
);
```

1.6. **Создать таблицу отключенных модификаторов**

```sql
CREATE TABLE menu_item_disabled_modifiers (
  id INT AUTO_INCREMENT PRIMARY KEY,
  item_id INT NOT NULL,
  modifier_id INT NOT NULL,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  FOREIGN KEY (item_id) REFERENCES menu_items(id) ON DELETE CASCADE,
  FOREIGN KEY (modifier_id) REFERENCES modifiers(id) ON DELETE CASCADE,
  UNIQUE KEY unique_item_modifier (item_id, modifier_id)
);
```

**Критерии выполнения:**

- ✅ Все таблицы созданы
- ✅ Индексы добавлены
- ✅ Foreign keys настроены
- ✅ Миграция выполняется без ошибок

---

#### Неделя 2: Модификация существующих таблиц и миграция данных

**Приоритет: КРИТИЧЕСКИЙ**
**Задачи:**
2.1. **Добавить поля КБЖУ в menu_items**

```sql
ALTER TABLE menu_items
ADD COLUMN composition TEXT AFTER description,
ADD COLUMN calories_per_100g DECIMAL(10, 2) NULL AFTER weight_unit,
ADD COLUMN proteins_per_100g DECIMAL(10, 2) NULL AFTER calories_per_100g,
ADD COLUMN fats_per_100g DECIMAL(10, 2) NULL AFTER proteins_per_100g,
ADD COLUMN carbs_per_100g DECIMAL(10, 2) NULL AFTER fats_per_100g,
ADD COLUMN calories_per_serving DECIMAL(10, 2) NULL AFTER carbs_per_100g,
ADD COLUMN proteins_per_serving DECIMAL(10, 2) NULL AFTER calories_per_serving,
ADD COLUMN fats_per_serving DECIMAL(10, 2) NULL AFTER proteins_per_serving,
ADD COLUMN carbs_per_serving DECIMAL(10, 2) NULL AFTER fats_per_serving;
```

2.2. **Добавить поля КБЖУ в item_variants**

```sql
ALTER TABLE item_variants
ADD COLUMN calories_per_100g DECIMAL(10, 2) NULL AFTER weight_unit,
ADD COLUMN proteins_per_100g DECIMAL(10, 2) NULL AFTER calories_per_100g,
ADD COLUMN fats_per_100g DECIMAL(10, 2) NULL AFTER proteins_per_100g,
ADD COLUMN carbs_per_100g DECIMAL(10, 2) NULL AFTER fats_per_100g,
ADD COLUMN calories_per_serving DECIMAL(10, 2) NULL AFTER carbs_per_100g,
ADD COLUMN proteins_per_serving DECIMAL(10, 2) NULL AFTER calories_per_serving,
ADD COLUMN fats_per_serving DECIMAL(10, 2) NULL AFTER proteins_per_serving,
ADD COLUMN carbs_per_serving DECIMAL(10, 2) NULL AFTER fats_per_serving;
```

2.3. **Добавить поля в modifiers**

```sql
ALTER TABLE modifiers
ADD COLUMN weight DECIMAL(10, 2) NULL AFTER price,
ADD COLUMN weight_unit ENUM('g', 'kg', 'ml', 'l', 'pcs') NULL AFTER weight,
ADD COLUMN image_url VARCHAR(255) NULL AFTER weight_unit;
```

2.4. **Добавить поля в modifier_groups**

```sql
ALTER TABLE modifier_groups
ADD COLUMN is_global BOOLEAN DEFAULT FALSE AFTER is_required,
ADD COLUMN min_selections INT DEFAULT 0 AFTER is_global,
ADD COLUMN max_selections INT DEFAULT 1 AFTER min_selections;
```

2.5. **Миграция существующих данных**

```sql
-- Перенести связи menu_items.category_id → menu_item_categories
INSERT INTO menu_item_categories (item_id, category_id)
SELECT id, category_id FROM menu_items WHERE category_id IS NOT NULL;
-- Перенести цены блюд в menu_item_prices
INSERT INTO menu_item_prices (item_id, city_id, fulfillment_type, price)
SELECT id, NULL, 'delivery', price FROM menu_items WHERE price IS NOT NULL
UNION ALL
SELECT id, NULL, 'pickup', price FROM menu_items WHERE price IS NOT NULL
UNION ALL
SELECT id, NULL, 'dine_in', price FROM menu_items WHERE price IS NOT NULL;
-- Перенести цены вариантов в menu_variant_prices
INSERT INTO menu_variant_prices (variant_id, city_id, fulfillment_type, price)
SELECT id, NULL, 'delivery', price FROM item_variants WHERE price IS NOT NULL
UNION ALL
SELECT id, NULL, 'pickup', price FROM item_variants WHERE price IS NOT NULL
UNION ALL
SELECT id, NULL, 'dine_in', price FROM item_variants WHERE price IS NOT NULL;
-- Создать привязку всех категорий ко всем городам
INSERT INTO menu_category_cities (category_id, city_id, is_active)
SELECT mc.id, c.id, TRUE
FROM menu_categories mc
CROSS JOIN cities c;
-- Создать привязку всех блюд ко всем городам
INSERT INTO menu_item_cities (item_id, city_id, is_active)
SELECT mi.id, c.id, TRUE
FROM menu_items mi
CROSS JOIN cities c;
-- Удалить старое поле category_id (после проверки)
-- ALTER TABLE menu_items DROP FOREIGN KEY menu_items_ibfk_1;
-- ALTER TABLE menu_items DROP COLUMN category_id;
```

**Критерии выполнения:**

- ✅ Все поля добавлены
- ✅ Данные мигрированы без потерь
- ✅ Тестовые запросы работают
- ✅ Создан backup перед миграцией

---

### Этап 2: Backend API (2 недели)

#### Неделя 3: Новые эндпоинты

**Приоритет: ВЫСОКИЙ**
**Задачи:**
3.1. **API для тегов**

- `GET /api/menu/admin/tags` - список всех тегов
- `POST /api/menu/admin/tags` - создание тега
- `PUT /api/menu/admin/tags/:id` - обновление тега
- `DELETE /api/menu/admin/tags/:id` - удаление тега
- `POST /api/menu/admin/items/:itemId/tags` - добавить тег к блюду
- `DELETE /api/menu/admin/items/:itemId/tags/:tagId` - удалить тег у блюда
  3.2. **API для множественных категорий**
- `POST /api/menu/admin/items/:itemId/categories` - добавить блюдо в категорию
- `DELETE /api/menu/admin/items/:itemId/categories/:categoryId` - удалить из категории
  3.3. **API управления доступностью**
- `POST /api/menu/admin/categories/:categoryId/cities` - включить категорию для города
- `PUT /api/menu/admin/categories/:categoryId/cities/:cityId` - изменить активность
- `POST /api/menu/admin/items/:itemId/cities` - включить блюдо для города
- `PUT /api/menu/admin/items/:itemId/cities/:cityId` - изменить активность
  3.4. **API управления ценами**
- `POST /api/menu/admin/items/:itemId/prices` - добавить/обновить цену блюда
- `GET /api/menu/admin/items/:itemId/prices` - получить цены блюда
- `POST /api/menu/admin/variants/:variantId/prices` - добавить/обновить цену варианта
- `POST /api/menu/admin/modifiers/:modifierId/variant-prices` - настроить цены модификатора
  3.5. **API стоп-листа**
- `GET /api/menu/admin/stop-list?branch_id={id}` - получить стоп-лист филиала
- `POST /api/menu/admin/stop-list` - добавить в стоп-лист
- `DELETE /api/menu/admin/stop-list/:id` - удалить из стоп-листа
  3.6. **API отключения модификаторов**
- `POST /api/menu/admin/items/:itemId/disabled-modifiers` - отключить модификатор
- `DELETE /api/menu/admin/items/:itemId/disabled-modifiers/:modifierId` - включить модификатор
  **Критерии выполнения:**
- ✅ Все эндпоинты реализованы
- ✅ Валидация входных данных
- ✅ Обработка ошибок
- ✅ Документация API (Postman/Swagger)

---

#### Неделя 4: Модификация существующих эндпоинтов

**Приоритет: ВЫСОКИЙ**
**Задачи:**
4.1. **Обновить GET /api/menu**

- Добавить параметры `branch_id` и `fulfillment_type`
- Фильтровать категории по городу (из menu_category_cities)
- Фильтровать блюда по городу (из menu_item_cities)
- Учитывать стоп-лист филиала
- Возвращать цены с учетом города и способа получения
- Возвращать КБЖУ для блюд и вариантов
- Возвращать теги
- Возвращать цены модификаторов по вариациям
  4.2. **Обновить POST /api/menu/admin/items**
- Добавить поля КБЖУ
- Добавить поле composition
- Добавить поддержку множественных категорий
- Добавить поддержку тегов
- Добавить поддержку городов
- Добавить поддержку цен
  4.3. **Обновить PUT /api/menu/admin/items/:id**
- Аналогично POST
  4.4. **Обновить POST /api/menu/admin/items/:itemId/variants**
- Добавить поля КБЖУ
- Добавить поддержку цен по городам
  4.5. **Обновить POST /api/menu/admin/modifier-groups**
- Добавить поле is_global
- Добавить поля min_selections, max_selections
  4.6. **Обновить POST /api/menu/admin/modifier-groups/:groupId/modifiers**
- Добавить поля weight, weight_unit, image_url
  **Критерии выполнения:**
- ✅ Обратная совместимость сохранена
- ✅ Новые поля опциональны
- ✅ Логика расчета цен работает корректно
- ✅ Тесты обновлены

---

### Этап 3: Admin Panel (2-3 недели)

#### Неделя 5: Новые экраны

**Приоритет: СРЕДНИЙ**
**Задачи:**
5.1. **Создать экран управления тегами**

- Файл: `admin-panel/src/views/MenuTags.vue`
- Таблица со списком тегов
- Модальное окно для создания/редактирования
- Функции: создать, редактировать, удалить
  5.2. **Создать экран стоп-листа**
- Файл: `admin-panel/src/views/MenuStopList.vue`
- Фильтр по филиалу
- Таблица с позициями в стоп-листе
- Форма добавления (тип, позиция, причина)
- Функция удаления из стоп-листа
  5.3. **Добавить роут в router**

```javascript
{
  path: '/menu/tags',
  name: 'MenuTags',
  component: () => import('../views/MenuTags.vue')
},
{
  path: '/menu/stop-list',
  name: 'MenuStopList',
  component: () => import('../views/MenuStopList.vue')
}
```

**Критерии выполнения:**

- ✅ Экраны созданы
- ✅ UI соответствует дизайн-системе
- ✅ CRUD операции работают
- ✅ Валидация форм

---

#### Неделя 6-7: Модификация существующих экранов

**Приоритет: ВЫСОКИЙ**
**Задачи:**
6.1. **Обновить MenuCategories.vue**

- Добавить управление доступностью по городам (чекбоксы)
- Обновить логику создания/редактирования
  6.2. **Обновить MenuItems.vue - создать табы**
  **Таб 1: Основная информация**
- Название, описание, состав
- Фото
- Вес с единицей измерения
- КБЖУ на 100г (калории, белки, жиры, углеводы)
- КБЖУ на порцию
- Порядок
- Активность
- Множественный выбор категорий (чекбоксы)
  **Таб 2: Вариации**
- Список вариаций с возможностью добавления/удаления
- Для каждой вариации: название, вес, КБЖУ (на 100г и на порцию)
- Модальное окно редактирования вариации
  **Таб 3: Модификаторы**
- Кнопка "Добавить глобальную группу" - выбор из списка
- Кнопка "Создать уникальную группу"
- Список добавленных групп
- Для глобальных групп - чекбоксы отключения отдельных модификаторов
- Кнопка настройки цен модификатора по вариациям
  **Модальное окно настройки цен модификатора:**
- Режим расчета: одинаковая/разные вручную/автоматически (% от первой)
- Для каждой вариации: цена, вес
  **Таб 4: Цены**
- Чекбокс "Использовать разные цены по городам"
- Если выключено: базовые цены для всех (доставка, самовывоз, зал)
- Если включено: выбор города и цены для него
- Цены для блюда (если нет вариантов)
- Цены для каждой вариации
  **Таб 5: Города**
- Чекбоксы доступности по городам
- Чекбоксы доступности по способам получения (доставка/самовывоз/зал)
  **Таб 6: Теги**
- Чекбоксы выбора тегов
- Кнопка создания нового тега
  6.3. **Обновить MenuModifiers.vue**
- Добавить поле "Глобальная группа" (чекбокс)
- Добавить поля "Минимум выборов" и "Максимум выборов"
- В форме модификатора добавить: вес, единицу измерения, загрузку фото
  **Критерии выполнения:**
- ✅ Все табы реализованы
- ✅ Формы работают корректно
- ✅ Валидация данных
- ✅ UI адаптивный
- ✅ Сохранение данных через API

---

### Этап 4: Client App (1-2 недели)

#### Неделя 8: Обновление клиентского приложения

**Приоритет: СРЕДНИЙ**
**Задачи:**
8.1. **Обновить Home.vue**

- Добавить фильтр по тегам (горизонтальная прокрутка кнопок)
- Отображать теги на карточках блюд (значки)
- Показывать индикацию стоп-листа ("Временно недоступно")
- Обновить API запрос (добавить branch_id, fulfillment_type)
  8.2. **Обновить ItemDetail.vue**
- Показывать теги под названием
- Показывать состав блюда
- Показывать КБЖУ (на 100г / на порцию)
- КБЖУ должно обновляться при выборе вариации
- Показывать фото модификаторов (если есть)
- Цены модификаторов должны меняться при выборе вариации
- Показывать недоступные модификаторы (disabled state)
  8.3. **Обновить stores/menu.js**
- Добавить загрузку тегов
- Обновить фильтрацию
  **Критерии выполнения:**
- ✅ Фильтр по тегам работает
- ✅ КБЖУ отображается корректно
- ✅ Стоп-лист показывается
- ✅ Цены модификаторов меняются
- ✅ UI плавный и отзывчивый

---

### Этап 5: Тестирование и доработки (1 неделя)

#### Неделя 9: Финальная проверка

**Приоритет: КРИТИЧЕСКИЙ**
**Задачи:**
9.1. **Функциональное тестирование**

- Проверить все CRUD операции
- Проверить валидацию
- Проверить расчет цен
- Проверить фильтрацию
- Проверить стоп-лист
  9.2. **Интеграционное тестирование**
- Проверить связь backend-admin-client
- Проверить кеширование
- Проверить инвалидацию кеша
- Проверить права доступа
  9.3. **Нагрузочное тестирование**
- Проверить производительность API
- Проверить скорость загрузки меню
- Оптимизировать SQL запросы
  9.4. **UI/UX тестирование**
- Проверить адаптивность
- Проверить доступность (a11y)
- Проверить на разных устройствах
  9.5. **Исправление багов**
- Создать список багов
- Приоритизировать
- Исправить критические
  **Критерии выполнения:**
- ✅ Все критические баги исправлены
- ✅ Производительность приемлема
- ✅ UI работает на всех устройствах
- ✅ Документация обновлена

---

### Этап 6: Развертывание и мониторинг (3-5 дней)

#### Подготовка к production

**Приоритет: КРИТИЧЕСКИЙ**
**Задачи:**
10.1. **Подготовка базы данных**

- Создать backup production БД
- Протестировать миграции на копии
- Создать rollback план
- Запустить миграции в maintenance window
  10.2. **Развертывание backend**
- Обновить код на сервере
- Проверить конфигурацию
- Перезапустить сервисы
- Мониторинг логов
  10.3. **Развертывание frontend**
- Собрать production build admin panel
- Собрать production build telegram miniapp
- Загрузить на CDN/хостинг
- Очистить кеш
  10.4. **Мониторинг**
- Настроить алерты на ошибки
- Мониторить производительность
- Следить за отзывами пользователей
- Быстро реагировать на проблемы
  **Критерии выполнения:**
- ✅ Развертывание без простоя
- ✅ Rollback план готов
- ✅ Мониторинг настроен
- ✅ Система стабильна

---

### Этап 7 (Опционально): Доработки фазы 2

**Приоритет: НИЗКИЙ**
**Задачи:**
11.1. **Массовые операции**

- Выбор нескольких позиций
- Применение изменений ко всем (цены, доступность)
  11.2. **История изменений**
- Логирование всех изменений
- Возможность отката
- Audit trail
  11.3. **Дополнительные фичи**
- Галерея фото (несколько изображений)
- Фото для вариаций
- Время продажи (расписание)
- Рекомендации (связанные товары)
- Сезонные блюда
- Импорт/экспорт меню
- Дублирование позиций

---

## 11. ПЛАН ПО НЕДЕЛЯМ (GANTT CHART)

```
Неделя 1:  [БД: Новые таблицы ████████████████]
Неделя 2:  [БД: Миграция данных ████████████████]
Неделя 3:  [Backend: Новые API ████████████████]
Неделя 4:  [Backend: Обновление API ████████████████]
Неделя 5:  [Admin: Новые экраны ████████████████]
Неделя 6:  [Admin: Табы MenuItems ████████████████]
Неделя 7:  [Admin: Обновление форм ████████████████]
Неделя 8:  [Client: Теги, КБЖУ, стоп-лист ████████████████]
Неделя 9:  [Тестирование ████████████████]
Неделя 10: [Развертывание ████████████]
```

---

## 12. РИСКИ И МИТИГАЦИЯ

### Критические риски:

1. **Риск:** Потеря данных при миграции БД
   - **Митигация:** Обязательный backup, тестирование на копии, rollback план
2. **Риск:** Нарушение работы текущего функционала
   - **Митигация:** Обратная совместимость, feature flags, постепенный rollout
3. **Риск:** Производительность при сложных запросах
   - **Митигация:** Оптимизация SQL, правильные индексы, кеширование
4. **Риск:** Несовместимость frontend-backend
   - **Митигация:** Версионирование API, тестирование интеграции

### Средние риски:

1. **Риск:** Увеличение времени разработки
   - **Митигация:** Реалистичные оценки, буфер времени, приоритизация
2. **Риск:** Сложность UI для администраторов
   - **Митигация:** UX-тестирование, обучающие материалы, онбординг

---

## 13. МЕТРИКИ УСПЕХА

**После завершения roadmap проект должен:**
✅ **Функциональность:**

- 100% покрытие требований документации
- Все 8 разделов документации реализованы
- 0 критических багов
  ✅ **Производительность:**
- Загрузка меню < 500мс
- Время отклика API < 200мс
- Кеш hit rate > 80%
  ✅ **Качество:**
- Unit тесты: 70%+ покрытие
- Integration тесты: критические сценарии
- 0 SQL injection уязвимостей
- Валидация всех входных данных
  ✅ **UX:**
- Время создания позиции < 2 минут
- Интуитивный интерфейс (без обучения)
- Адаптивный дизайн на всех устройствах

---

## 14. ИТОГОВЫЕ РЕКОМЕНДАЦИИ

### Немедленные действия:

1. ✅ **Создать ветку `feature/menu-system-2.0`**
2. ✅ **Настроить staging окружение для тестирования**
3. ✅ **Создать backup production БД**
4. ✅ **Начать с миграции БД (Этап 1)**

### Приоритеты:

🔴 **КРИТИЧНО** - миграция БД, основные API, обратная совместимость  
🟠 **ВАЖНО** - admin panel табы, управление ценами  
🟡 **ЖЕЛАТЕЛЬНО** - теги, КБЖУ, стоп-лист  
🟢 **ОПЦИОНАЛЬНО** - массовые операции, история изменений

### Долгосрочная стратегия:

- Регулярные релизы (каждые 1-2 недели)
- Непрерывное тестирование
- Сбор обратной связи от пользователей
- Итеративные улучшения

---

**Конец документа**
