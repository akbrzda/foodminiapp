# Техническое задание: Страница "Текущая смена"

## 1. Общее описание

### 1.1 Назначение
Страница "Текущая смена" предназначена для оперативного управления заказами в течение текущей рабочей смены. Страница обеспечивает визуализацию заказов на карте, быстрое переключение статусов и мониторинг текущей операционной активности филиала.

### 1.2 Отличия от существующей страницы "Заказы"
- Показывает только заказы текущей смены (без возможности изменения периода)
- Полноэкранный режим без сайдбара
- Интеграция с интерактивной картой
- Упрощенное управление статусами
- Real-time обновления с звуковыми уведомлениями

---

## 2. Определение смены

### 2.1 Временные рамки
- **Начало смены:** 05:00 местного времени города
- **Окончание смены:** 04:59 следующего дня местного времени города
- **Часовой пояс:** Каждый город работает в своем часовом поясе

### 2.2 Логика отображения
- При открытии страницы загружаются только заказы текущей смены
- При переходе на следующую смену (после 05:00) страница автоматически "обнуляется"
- Заказы предыдущей смены не отображаются на этой странице
- Изменение периода, фильтрация по датам недоступны

### 2.3 Критерий принадлежности заказа к смене
Заказ относится к текущей смене если:
```
created_at >= CURRENT_DATE 05:00:00 (local timezone)
AND created_at < NEXT_DATE 05:00:00 (local timezone)
```

---

## 3. Архитектура страницы

### 3.1 Общая структура
```
┌─────────────────────────────────────────────────────┐
│ Топбар (Header)                                      │
│ - Логотип                                            │
│ - Название страницы "Текущая смена"                  │
│ - Селектор филиала                                   │
│ - Кнопка "Админ-панель" (переход в новой вкладке)   │
└─────────────────────────────────────────────────────┘
┌──────────────────────┬──────────────────────────────┐
│                      │                              │
│  Список заказов      │         Карта                │
│  (40% ширины)        │      (60% ширины)            │
│                      │                              │
│  ┌─────────────┐    │  - Полигоны зон доставки     │
│  │ Вкладка 1   │    │  - Точки заказов             │
│  │ Активные    │    │  - Линии маршрутов           │
│  │ Badge: X    │    │  - Кнопка управления         │
│  └─────────────┘    │    полигонами                │
│  ┌─────────────┐    │                              │
│  │ Вкладка 2   │    │                              │
│  │ Завершенные │    │                              │
│  │ Badge: X    │    │                              │
│  └─────────────┘    │                              │
│  ┌─────────────┐    │                              │
│  │ Вкладка 3   │    │                              │
│  │ Поиск       │    │                              │
│  └─────────────┘    │                              │
│                      │                              │
│  [Фильтр: Все ▼]    │                              │
│                      │                              │
│  [Поле поиска]       │                              │
│  (только в Поиске)   │                              │
│                      │                              │
│  [Карточки заказов] │                              │
│  (бесконечный       │                              │
│   скроллинг)        │                              │
│                      │                              │
└──────────────────────┴──────────────────────────────┘
```

### 3.2 Технические характеристики
- **Режим отображения:** Полноэкранный (без сайдбара)
- **Адаптивность:** Desktop only (минимум 1280px)
- **Разделение экрана:** 40% список заказов / 60% карта
- **Фреймворк UI:** Shadcn UI + Vue 3
- **Библиотека карт:** Leaflet.js + OpenStreetMap

---

## 4. Топбар (Header)

### 4.1 Элементы топбара
1. **Логотип** (слева)
2. **Название страницы** "Текущая смена"
3. **Селектор филиала** (Dropdown)
4. **Кнопка "Админ-панель"** (справа)

### 4.2 Селектор филиала
**Компонент:** Select (Shadcn UI)

**Поведение:**
- Показывает только филиалы доступные текущему пользователю
- При первом заходе: если филиал не выбран, показывать модальное окно с предложением выбрать филиал
- Сохранять выбранный филиал в localStorage: `shift_selected_branch_id`
- При смене филиала:
  - Перезагрузить список заказов
  - Обновить полигоны на карте
  - Сбросить выбранный заказ (свернуть развернутые карточки)

**Структура данных:**
```javascript
{
  id: number,
  name: string,
  city_id: number,
  city_name: string,
  address: string,
  latitude: number,
  longitude: number
}
```

### 4.3 Кнопка "Админ-панель"
**Компонент:** Button (Shadcn UI, variant="outline")

**Поведение:**
- Открывает главную админ-панель в новой вкладке браузера
- URL: `/admin/dashboard` (или основной роут админки)
- Target: `_blank`

---

## 5. Вкладки со статусами

### 5.1 Структура вкладок
**Компонент:** Tabs (Shadcn UI)

**Вкладка 1: "Активные" (по умолчанию)**
- Включает статусы:
  - `pending` (Новый)
  - `confirmed` (Принят)
  - `preparing` (Готовится)
  - `ready` (Готов / Готов к выдаче)
- Badge показывает: **общее количество заказов в этой вкладке**
- Отображается список заказов без поля поиска

**Вкладка 2: "Завершенные"**
- Включает статусы:
  - `delivering` (В пути) - только для доставки
  - `completed` (Доставлен / Выдан)
  - `cancelled` (Отменен)
- Badge показывает: **количество заказов со статусом "В пути" (delivering)**
- Отображается список заказов без поля поиска

**Вкладка 3: "Поиск"**
- Включает ВСЕ заказы (активные + завершенные)
- Badge не отображается
- Содержит:
  - Поле ввода для поиска (вверху списка)
  - Список заказов, отфильтрованных по запросу

### 5.2 Визуальный дизайн вкладок
**Активная вкладка:**
- Border-bottom: 2px solid `#FFD200`
- Текст: Bold, `#000000`
- Badge: Фон `#FFD200`, текст `#000000`

**Неактивная вкладка:**
- Текст: Regular, `#666666`
- Badge: Фон `#E0E0E0`, текст `#666666`

### 5.3 Расчет значений Badge

**Вкладка "Активные":**
```javascript
const activeBadgeCount = orders.filter(order => 
  ['pending', 'confirmed', 'preparing', 'ready'].includes(order.status)
).length
```

**Вкладка "Завершенные":**
```javascript
const deliveringCount = orders.filter(order => 
  order.status === 'delivering'
).length
```

**Вкладка "Поиск":**
- Badge отсутствует

### 5.4 Фильтр типа заказа

**Расположение:** Рядом с вкладками (справа от вкладок или между вкладками и списком)

**Компонент:** Button + Dropdown Menu (Shadcn UI)

**Визуальное оформление:**
- Кнопка с текстом выбранного фильтра + иконка стрелки вниз
- Например: "Все ▼" или "Доставка ▼"

**Опции в Dropdown:**
- ✓ **Все** (по умолчанию, выбрано галочкой)
- **Доставка** (фильтр: `order_type = 'delivery'`)
- **Самовывоз** (фильтр: `order_type = 'pickup'`)

**Поведение:**
- Одиночный выбор (radio-style)
- При выборе:
  - Закрыть dropdown
  - Обновить текст на кнопке
  - Отфильтровать список заказов в текущей вкладке
- Фильтр применяется независимо на каждой вкладке:
  - Во вкладке "Активные" фильтрует активные заказы
  - Во вкладке "Завершенные" фильтрует завершенные заказы
  - Во вкладке "Поиск" фильтрует все заказы + поисковый запрос
- Состояние фильтра НЕ сохраняется (сбрасывается на "Все" при обновлении страницы)
- При переключении вкладок фильтр остается активным

**Визуальная индикация:**
- Выбранный пункт отмечен галочкой (checkmark)
- Кнопка фильтра всегда показывает текущий выбранный вариант

---

## 6. Список заказов

### 6.1 Общие параметры
- **Ширина:** 40% экрана
- **Скроллинг:** Бесконечный вертикальный скроллинг
- **Пагинация:** Отсутствует (все заказы загружаются сразу)
- **Сортировка:** Новые заказы сверху (по `created_at DESC`)

### 6.2 Компактная карточка заказа

**Структура:**
```
┌──────────────────────────────────────────────┐
│ #33264  ⏰ 19:32 до 20:42      [Готовится]  │ ← Строка 1
│ 📍 Сургут, проезд Дружбы, 17, кв. 33...     │ ← Строка 2
│ 📞 89125115313                               │ ← Строка 3
│ 💳 К оплате: 2150₽ наличными (3шт)          │ ← Строка 4
└──────────────────────────────────────────────┘
```

**Строка 1:**
- Номер заказа (слева): `#` + `order_number` (жирный шрифт)
- Иконка часов + временной диапазон: `created_at` до `deadline_time`
- Badge со статусом (справа): текст статуса на русском

**Строка 2:**
- Иконка локации + адрес доставки (для `order_type = 'delivery'`)
- Иконка магазина + название филиала (для `order_type = 'pickup'`)
- Текст обрезается с `...` если не влезает

**Строка 3:**
- Иконка телефона + номер телефона клиента

**Строка 4:**
- Иконка оплаты + текст: "К оплате: `total_amount`₽ `payment_method_label` (`change_label`)"
  - Для наличных: "наличными (3шт)" - где 3шт = количество позиций
  - Для карты: "картой (3шт)"

**Визуальное состояние:**
- **Обычное:** Фон `#FFFFFF`, border `1px solid #E0E0E0`, border-radius `12px`
- **Просроченное:** Фон `#FFFFFF`, border `2px solid #FF0000`
- **Выбранное (развернутое):** Border `2px solid #FFD200`, тень увеличена

### 6.3 Развернутая карточка заказа

При клике на компактную карточку она раскрывается с добавлением блоков:

**Добавляемые блоки:**

**Блок: Список позиций**
```
┌──────────────────────────────────────────────┐
│ Большая из двух половин  1305₽              │ ← Позиция
│ Дьябло, Пиццбургер, Соус Сырный 20г x3...   │ ← Модификаторы
│                                              │
│ Чикен байтс большие  495₽                   │
│                                              │
│ Картофель фри с беконом и соусом  350₽      │
│ Вилка одноразовая                            │
└──────────────────────────────────────────────┘
```

**Формат отображения позиции:**
- Название + цена (жирный шрифт)
- Модификаторы на отдельной строке (серый текст, через запятую)
- Разделитель между позициями: тонкая линия `#E0E0E0`

**Блок: Стоимость доставки** (если `delivery_fee > 0`)
```
Доставка: 0₽ (или конкретная сумма)
```

**Блок: Итоговая сумма** (переносится из строки 4 компактной карточки)
```
Итого: 2150₽
```

**Блок: Кнопка смены статуса**
```
┌──────────────────────────────────────────────┐
│          [Принять заказ]                     │ ← Кнопка
└──────────────────────────────────────────────┘
```

**Компонент:** Button (Shadcn UI, variant="default", full width)

**Текст кнопки зависит от текущего статуса:**

Доставка:
- `pending` → "Принять заказ"
- `confirmed` → "Отправить в готовку"
- `preparing` → "Готов к выдаче"
- `ready` → "Передать курьеру"
- `delivering` → "Заказ доставлен"

Самовывоз:
- `pending` → "Принять заказ"
- `confirmed` → "Отправить в готовку"
- `preparing` → "Готов к выдаче"
- `ready` → "Выдать заказ"

**Дополнительная кнопка отмены:**
```
┌──────────────────────────────────────────────┐
│          [Отменить заказ]                    │ ← Вторая кнопка
└──────────────────────────────────────────────┘
```

**Компонент:** Button (Shadcn UI, variant="destructive" или "outline", full width)

**Доступность:** Во всех статусах кроме `completed` и `cancelled`

### 6.4 Поведение при клике на карточку

**Клик на компактную карточку:**
1. Развернуть карточку (добавить скрытые блоки с анимацией)
2. Если `order_type = 'delivery'`:
   - Показать точку ресторана (красная) на карте
   - Показать точку адреса доставки (синяя) на карте
   - Нарисовать линию между точками
   - Центрировать карту чтобы обе точки были видны
3. Если `order_type = 'pickup'`:
   - Ничего не происходит с картой

**Клик на развернутую карточку:**
1. Свернуть карточку (скрыть дополнительные блоки с анимацией)
2. Удалить точки и линию с карты

**Клик на кнопку смены статуса:**
1. Отправить запрос на сервер: `PUT /api/admin/orders/{id}/status`
2. При успешном ответе:
   - Обновить статус заказа в клиентском состоянии
   - Свернуть карточку
   - Переместить карточку в соответствующую вкладку (если изменилась вкладка)
   - Обновить значения Badge
   - Удалить точки и линию с карты
3. При ошибке:
   - Показать Toast с текстом ошибки

**Клик на кнопку отмены:**
1. Показать Dialog подтверждения: "Отменить заказ #33264?"
2. При подтверждении:
   - Отправить запрос: `PUT /api/admin/orders/{id}/cancel`
   - Обновить статус на `cancelled`
   - Переместить в вкладку "Завершенные"
   - Свернуть карточку
3. При отмене: закрыть Dialog

### 6.5 Цветовая индикация статусов (Badge)

**Доставка:**
- `pending` (Новый): фон `#3B82F6` (синий), текст белый
- `confirmed` (Принят): фон `#10B981` (зеленый), текст белый
- `preparing` (Готовится): фон `#F59E0B` (оранжевый), текст белый
- `ready` (Готов): фон `#8B5CF6` (фиолетовый), текст белый
- `delivering` (В пути): фон `#FFD200` (желтый), текст черный
- `completed` (Доставлен): фон `#6B7280` (серый), текст белый
- `cancelled` (Отменен): фон `#EF4444` (красный), текст белый

**Самовывоз:**
- `pending` (Новый): фон `#3B82F6` (синий), текст белый
- `confirmed` (Принят): фон `#10B981` (зеленый), текст белый
- `preparing` (Готовится): фон `#F59E0B` (оранжевый), текст белый
- `ready` (Готов к выдаче): фон `#8B5CF6` (фиолетовый), текст белый
- `completed` (Выдан): фон `#6B7280` (серый), текст белый
- `cancelled` (Отменен): фон `#EF4444` (красный), текст белый

### 6.6 Пустое состояние

**Когда отображать:**
- Во вкладке нет заказов (после применения фильтров)
- Во вкладке "Поиск" нет результатов по запросу

**Визуальное отображение:**
```
┌──────────────────────────────────────────────┐
│                                              │
│               [Иконка пустой коробки]        │
│                                              │
│           Нет заказов                        │
│                                              │
└──────────────────────────────────────────────┘
```

**Компоненты:** Иконка (lucide-vue: `PackageOpen`), текст (серый цвет)

**Для вкладки "Поиск":**
- Если нет поискового запроса: "Введите запрос для поиска"
- Если есть запрос, но нет результатов: "Ничего не найдено"

---

## 6.7 Вкладка "Поиск"

### 6.7.1 Назначение
Вкладка "Поиск" предоставляет возможность поиска заказов по всем заказам смены (активные + завершенные) с фильтрацией в реальном времени.

### 6.7.2 Структура вкладки

**Верхняя часть (фиксированная):**
- Поле ввода для поиска (Input компонент)
  - Placeholder: "Поиск по номеру, телефону или адресу"
  - Иконка лупы слева
  - Кнопка очистки (X) справа (появляется при вводе текста)

**Нижняя часть (скроллируемая):**
- Список карточек заказов, соответствующих запросу

### 6.7.3 Поле поиска

**Компонент:** Input (Shadcn UI)

**Параметры:**
- Autofocus при переключении на вкладку "Поиск"
- Debounce: 300ms
- Минимальная длина запроса: 1 символ

**Поиск выполняется по полям:**
- Номер заказа (`order_number`) - точное совпадение или начало строки
- Номер телефона (`customer_phone`) - частичное совпадение
- Адрес доставки (`delivery_address`) - частичное совпадение, регистронезависимо

### 6.7.4 Отображение результатов

**Список результатов:**
- Карточки заказов в том же формате что и в других вкладках
- Сортировка: новые заказы сверху (по `created_at DESC`)
- Применяется текущий фильтр типа заказа (если выбран "Доставка" или "Самовывоз")

**Поведение при клике на карточку:**
- Карточка раскрывается на месте (без прокрутки или перехода)
- Если `order_type = 'delivery'`: показать точки и линию на карте
- Если `order_type = 'pickup'`: карта не меняется

**Пустые состояния:**

Если `searchQuery` пустой:
```
┌──────────────────────────────────────────────┐
│                                              │
│               [Иконка лупы]                  │
│                                              │
│      Введите запрос для поиска               │
│                                              │
└──────────────────────────────────────────────┘
```

Если есть запрос, но нет результатов:
```
┌──────────────────────────────────────────────┐
│                                              │
│               [Иконка пустой коробки]        │
│                                              │
│           Ничего не найдено                  │
│    Попробуйте изменить запрос                │
│                                              │
└──────────────────────────────────────────────┘
```

### 6.7.5 Очистка поиска

**Кнопка очистки (X):**
- Появляется справа в поле ввода когда введен текст
- При клике:
  - Очистить `searchQuery`
  - Вернуть фокус на поле ввода
  - Отобразить пустое состояние "Введите запрос для поиска"

**Сочетание клавиш:**
- `Esc` - очистить поле поиска (если поле в фокусе)

### 6.7.6 Взаимодействие с фильтром типа заказа

Фильтр типа заказа работает вместе с поиском:
- Если выбран "Доставка": показывать только заказы с `order_type = 'delivery'` из результатов поиска
- Если выбран "Самовывоз": показывать только заказы с `order_type = 'pickup'` из результатов поиска
- Если выбран "Все": показывать все результаты поиска

### 6.7.7 Переключение между вкладками

**При переходе на вкладку "Поиск":**
- Установить фокус на поле ввода
- Если был введен запрос ранее: сохранить и показать результаты
- Если запрос был очищен: показать пустое состояние

**При переходе с вкладки "Поиск" на другую:**
- Сохранить текущий поисковый запрос (не очищать)
- При возврате на вкладку "Поиск": восстановить запрос и результаты

**Опционально:** Добавить возможность очистки запроса при переключении вкладок (настройка UX)

---

## 7. Карта

### 7.1 Технические параметры
- **Библиотека:** Leaflet.js v1.9+
- **Тайлы:** OpenStreetMap
- **Ширина:** 60% экрана
- **Высота:** 100vh минус высота топбара

### 7.2 Элементы карты

**Постоянные элементы:**
- Карта OpenStreetMap (базовый слой)
- Кнопка управления полигонами (верхний правый угол)

**Динамические элементы:**
- Полигоны зон доставки (опционально)
- Точка ресторана (красная) - при выборе заказа
- Точка адреса доставки (синяя) - при выборе заказа
- Линия маршрута (синяя) - при выборе заказа

### 7.3 Полигоны зон доставки

**Источник данных:** 
- API endpoint: `GET /api/admin/delivery-zones?branch_id={id}`
- Формат: GeoJSON с типом `Polygon`

**Визуальное оформление:**
- Цвет заливки: `#3B82F6` (синий)
- Прозрачность заливки: 20% (`opacity: 0.2`)
- Цвет границы: `#3B82F6`
- Толщина границы: 2px
- Прозрачность границы: 80% (`opacity: 0.8`)

**Кнопка управления полигонами:**
- **Позиция:** Правый верхний угол карты
- **Компонент:** Button (Shadcn UI, размер: icon, variant="outline")
- **Иконки:**
  - Полигоны скрыты: `EyeOff`
  - Полигоны видимы: `Eye`
- **Состояние сохраняется:** localStorage: `shift_polygons_visible`

**Поведение:**
- При клике на кнопку:
  - Переключить видимость полигонов
  - Сохранить состояние в localStorage
  - Если полигоны включены: центрировать карту чтобы все полигоны были видны
- При смене филиала:
  - Загрузить полигоны нового филиала
  - Применить сохраненное состояние видимости

### 7.4 Точки и линии заказа

**Красная точка (ресторан):**
- **Маркер:** Leaflet marker с custom icon
- **Иконка:** Пин красного цвета или SVG иконка ресторана
- **Координаты:** `branch.latitude`, `branch.longitude`

**Синяя точка (адрес доставки):**
- **Маркер:** Leaflet marker с custom icon
- **Иконка:** Пин синего цвета
- **Координаты:** `order.delivery_latitude`, `order.delivery_longitude`

**Линия маршрута:**
- **Тип:** Leaflet Polyline
- **Цвет:** `#3B82F6` (синий)
- **Толщина:** 3px
- **Стиль:** Пунктирная линия (`dashArray: '10, 5'`)
- **Координаты:** От ресторана до адреса доставки

**Центрирование карты:**
- При отображении заказа карта должна центрироваться так, чтобы:
  - Обе точки (ресторан и адрес) были видны
  - Оставался отступ от краев карты (padding: 50px)
- Использовать: `map.fitBounds([[lat1, lng1], [lat2, lng2]], { padding: [50, 50] })`

**Удаление элементов:**
- При сворачивании карточки заказа:
  - Удалить красную точку
  - Удалить синюю точку
  - Удалить линию маршрута
- При выборе другого заказа:
  - Удалить элементы предыдущего заказа
  - Отобразить элементы нового заказа

### 7.5 Взаимодействие с картой

**Зум:**
- Начальный уровень зума: 12
- Минимальный зум: 10
- Максимальный зум: 18
- Контролы зума: стандартные Leaflet

**Перемещение:**
- Пользователь может свободно перемещать карту
- При выборе нового заказа карта автоматически центрируется

**Клики по карте:**
- Клик на пустое место карты: ничего не происходит
- Клик на полигон: ничего не происходит
- Клик на точку заказа: прокрутка к карточке заказа в списке и её раскрытие

---

## 8. Real-time обновления

### 8.1 WebSocket соединение

**Подключение:**
- URL: `wss://your-domain.com/ws/shift-orders`
- Аутентификация: JWT токен в query параметре или header
- Комната: Подписка на события выбранного филиала

**События для подписки:**
- `order.created` - новый заказ создан
- `order.updated` - заказ обновлен (статус изменен)
- `order.cancelled` - заказ отменен

### 8.2 Обработка событий

**Событие: `order.created`**

Действия:
1. Проверить что заказ относится к выбранному филиалу
2. Добавить заказ в начало списка соответствующей вкладки
3. Обновить значения Badge
4. Воспроизвести звуковое уведомление
5. Показать Browser notification (если разрешено)

**Событие: `order.updated`**

Действия:
1. Найти заказ в списке по `order_id`
2. Обновить данные заказа
3. Если статус изменился:
   - Переместить заказ в соответствующую вкладку
   - Обновить значения Badge
4. Если заказ был развернут и текущий пользователь НЕ инициатор изменения:
   - Свернуть карточку
   - Удалить точки и линию с карты

**Событие: `order.cancelled`**

Действия:
1. Найти заказ в списке
2. Обновить статус на `cancelled`
3. Переместить в вкладку "Завершенные"
4. Обновить значения Badge
5. Если заказ был развернут: свернуть и удалить элементы с карты

### 8.3 Звуковые уведомления

**Условие воспроизведения:**
- Только при событии `order.created`
- Только если страница активна (document.visibilityState === 'visible')

**Звуковой файл:**
- Формат: MP3 или OGG
- Длительность: 1-2 секунды
- Громкость: Средняя (можно настраивать)
- Расположение: `/public/sounds/new-order.mp3`

## 12. Стили и дизайн

### 12.1 Цветовая палитра

Использовать цвета из `design_system.md`:

**Основные:**
- Primary: `#FFD200`
- Background: `#F5F5F5`
- Card Background: `#FFFFFF`
- Text Primary: `#000000`
- Text Secondary: `#666666`
- Border: `#E0E0E0`

**Семантические:**
- Success: `#4CAF50`
- Error: `#FF0000`
- Warning: `#FFA500`

**Статусы (как в разделе 6.5):**
- Синий: `#3B82F6`
- Зеленый: `#10B981`
- Оранжевый: `#F59E0B`
- Фиолетовый: `#8B5CF6`
- Желтый: `#FFD200`
- Серый: `#6B7280`
- Красный: `#EF4444`

### 12.2 Типографика

**Использовать из `design_system.md`:**
- H1: 28px, Bold
- H2: 20px, Semibold
- H3: 17px, Semibold
- Body: 15px, Regular
- Caption: 13px, Regular
- Small: 11px, Regular

### 12.3 Компоненты Shadcn UI

**Использовать компоненты:**
- Button
- Badge
- Tabs
- Select
- Dialog
- Input
- Toggle Group
- Toast (Sonner)
- Command (для поиска)

**Кастомизация:**
- Primary кнопки: фон `#FFD200`, текст `#000000`
- Border radius: 12px (карточки), 8px (кнопки)
- Shadows: используть стандартные Tailwind

### 12.4 Анимации

**Раскрытие/сворачивание карточки:**
- Transition: `all 200ms ease-out`
- Transform: scale для плавного появления блоков

**Перемещение карточки между вкладками:**
- Без анимации, просто исчезает/появляется

**Появление новой карточки (WebSocket):**
- Fade in + slide down
- Duration: 300ms
- Easing: ease-out

### 12.5 Адаптивность

**Минимальная ширина экрана:** 1280px

**Брейкпоинты:**
- 1280px - 1440px: Стандартное отображение (40% / 60%)
- 1440px+: Можно увеличить ширину списка до 45%

**Мобильная версия:** НЕ поддерживается (desktop only)

---

## 13. Пользовательские сценарии

### 13.1 Сценарий 1: Обработка нового заказа

1. Менеджер открывает страницу "Текущая смена"
2. Выбирает свой филиал (если не выбран)
3. Появляется новый заказ через WebSocket
   - Звучит звуковое уведомление
   - Карточка появляется вверху списка во вкладке "Активные"
   - Badge обновляется
4. Менеджер кликает на карточку заказа
   - Карточка раскрывается
   - На карте появляются точки и линия маршрута
5. Менеджер проверяет состав заказа
6. Нажимает кнопку "Принять заказ"
   - Статус меняется на `confirmed`
   - Карточка сворачивается
   - Точки и линия исчезают с карты
7. Когда заказ готовится, менеджер снова кликает на карточку
8. Нажимает "Отправить в готовку"
   - Статус меняется на `preparing`
9. Продолжает менять статусы до завершения заказа

### 13.2 Сценарий 2: Отмена заказа

1. Менеджер находит заказ в списке
2. Кликает на карточку для раскрытия
3. Нажимает кнопку "Отменить заказ"
4. Появляется Dialog с подтверждением
5. Подтверждает отмену
6. Заказ перемещается во вкладку "Завершенные"
7. Badge обновляется

### 13.3 Сценарий 3: Поиск заказа

1. Менеджер переключается на вкладку "Поиск"
2. Поле ввода автоматически получает фокус
3. Вводит номер телефона клиента: "89125115313"
4. После 300ms задержки появляются результаты поиска
5. Видит карточку заказа клиента
6. Кликает на карточку заказа
7. Карточка раскрывается на месте
8. На карте появляются точки и линия маршрута
9. Может сразу обработать заказ (изменить статус)
10. При необходимости очищает поиск кнопкой X или клавишей Esc

### 13.4 Сценарий 4: Работа с картой

1. Менеджер переключает вкладку на "Завершенные"
2. Находит заказ со статусом "В пути"
3. Кликает на карточку
4. Карта показывает текущий маршрут доставки
5. Менеджер может позвонить клиенту по номеру на карточке
6. Когда курьер доставил заказ, менеджер нажимает "Заказ доставлен"
7. Заказ остается в "Завершенные", статус меняется на `completed`

### 13.5 Сценарий 5: Переключение филиалов

1. Менеджер работает в филиале "Центр"
2. Нужно проверить заказы филиала "Восточный"
3. Кликает на селектор филиала в топбаре
4. Выбирает "Восточный"
5. Список заказов перезагружается
6. Карта центрируется на новом филиале
7. Полигоны обновляются для нового филиала

---

## 14. Обработка ошибок

### 14.1 Ошибки загрузки данных

**Сценарий:** Не удалось загрузить заказы смены

**Действия:**
1. Показать Toast с ошибкой: "Не удалось загрузить заказы. Попробуйте обновить страницу."
2. Отобразить Empty State с кнопкой "Повторить попытку"
3. При клике на кнопку: повторить запрос

### 14.2 Ошибки WebSocket

**Сценарий:** WebSocket соединение разорвано

**Действия:**
1. Показать небольшое уведомление внизу экрана: "Соединение потеряно. Переподключение..."
2. Попытаться переподключиться (exponential backoff: 1s, 2s, 4s, 8s, 16s)
3. При успешном переподключении: скрыть уведомление, перезагрузить заказы
4. После 5 неудачных попыток: показать ошибку и кнопку "Обновить страницу"

### 14.3 Ошибки смены статуса

**Сценарий:** Не удалось изменить статус заказа

**Возможные причины:**
- 400: Неверный переход статуса
- 403: Нет прав на этот филиал
- 409: Заказ уже изменен другим пользователем
- 500: Внутренняя ошибка сервера

**Действия:**
1. Показать Toast с текстом ошибки
2. Не менять статус в UI
3. Для 409: перезагрузить данные заказа с сервера

### 14.4 Ошибки карты

**Сценарий:** Не удалось загрузить тайлы карты

**Действия:**
1. Показать заглушку на месте карты: "Карта недоступна"
2. Список заказов продолжает работать
3. Кнопка "Попробовать снова" перезагружает карту

**Сценарий:** Некорректные координаты заказа

**Действия:**
1. Не показывать точку на карте
2. В карточке заказа показать предупреждение: "Адрес не найден на карте"
3. Все остальные функции карточки работают нормально

---

## 15. Производительность и оптимизация

### 15.1 Загрузка данных

**Стратегия загрузки:**
- При открытии страницы: загрузить все заказы смены одним запросом
- Нет пагинации (предполагается до 100-200 заказов за смену)
- Использовать кеширование в памяти

**Оптимизация запросов:**
- Использовать compressed response (gzip)
- Минимизировать размер payload (только нужные поля)
- При WebSocket событиях: получать полный объект заказа (избежать дополнительных запросов)

### 15.2 Рендеринг списка

**Виртуализация:**
- Использовать `vue-virtual-scroller` для больших списков (если >100 заказов)
- Рендерить только видимые карточки + буфер

**Реактивность:**
- Использовать `shallowRef` для больших массивов
- Избегать глубоких вложенных computed

### 15.3 Карта

**Оптимизация Leaflet:**
- Использовать Canvas Renderer для большого количества маркеров
- Ленивая загрузка тайлов
- Отключать ненужные контролы

**Управление слоями:**
- Переиспользовать маркеры вместо создания новых
- Группировать полигоны в Feature Group
- Удалять неиспользуемые слои из памяти

### 15.4 WebSocket

**Оптимизация:**
- Использовать бинарные сообщения (MessagePack) вместо JSON
- Батчинг событий: группировать несколько событий в одно сообщение
- Debounce частых обновлений

### 15.5 Звуковые уведомления

**Оптимизация:**
- Предзагрузка звукового файла при загрузке страницы
- Использовать Web Audio API для лучшего контроля
- Ограничить количество одновременных воспроизведений (макс. 1)

---

## 17. Развертывание и настройка

### 17.3 Права доступа

**Роли с доступом к странице:**
- Администратор (все филиалы)
- Менеджер (только свои филиалы)

## 19. Чеклист перед запуском

### 19.1 Backend готовность

- [ ] API endpoint `/admin/shift-orders` реализован
- [ ] API endpoint `/admin/orders/{id}/status` реализован
- [ ] API endpoint `/admin/orders/{id}/cancel` реализован
- [ ] API endpoint `/admin/delivery-zones` реализован
- [ ] WebSocket сервер настроен
- [ ] WebSocket события `order.created`, `order.updated`, `order.cancelled` реализованы
- [ ] Права доступа настроены (admin, manager)
- [ ] Часовые пояса городов учтены в логике смены
- [ ] Расчет `deadline_time` работает корректно

### 19.2 Frontend готовность

- [ ] Shadcn UI компоненты установлены
- [ ] Leaflet.js подключен и настроен
- [ ] OpenStreetMap тайлы загружаются
- [ ] Звуковой файл уведомления добавлен в `/public/sounds/`
- [ ] Browser Notifications API протестирован
- [ ] localStorage для сохранения настроек работает
- [ ] WebSocket соединение стабильно
- [ ] Все компоненты адаптированы под дизайн-систему
- [ ] TypeScript типы определены

### 19.3 Функциональность

- [ ] Загрузка заказов смены работает
- [ ] Фильтр типа заказа (Dropdown) работает на всех вкладках
- [ ] Вкладка "Поиск" работает с поиском в реальном времени
- [ ] Поиск по номеру/телефону/адресу работает
- [ ] Пустые состояния вкладки "Поиск" отображаются корректно
- [ ] Очистка поиска кнопкой X и клавишей Esc работает
- [ ] Автофокус на поле поиска при переключении на вкладку "Поиск"
- [ ] Фильтр применяется вместе с поиском
- [ ] Смена статуса работает последовательно
- [ ] Отмена заказа работает из любого статуса
- [ ] Карточка раскрывается/сворачивается корректно
- [ ] Карта отображает точки и линии правильно
- [ ] Полигоны загружаются и отображаются
- [ ] Переключение филиалов работает
- [ ] WebSocket события обрабатываются
- [ ] Звуковые уведомления работают
- [ ] Browser notifications запрашиваются и работают
- [ ] Badge значения обновляются корректно
- [ ] Заказы перемещаются между вкладками

### 19.4 UX и дизайн

- [ ] Просроченные заказы выделены красной рамкой
- [ ] Статусы отображаются правильными цветами
- [ ] Шрифты и размеры соответствуют дизайн-системе
- [ ] Анимации плавные и не тормозят
- [ ] Пустые состояния отображаются корректно
- [ ] Ошибки показываются через Toast
- [ ] Кнопки имеют правильные состояния (loading, disabled)
- [ ] Топбар корректно выглядит

### 19.5 Производительность

- [ ] Страница загружается за < 2 секунды
- [ ] Скроллинг списка плавный (60 FPS)
- [ ] WebSocket события обрабатываются за < 100ms
- [ ] Карта не лагает при перемещении
- [ ] Memory leaks отсутствуют