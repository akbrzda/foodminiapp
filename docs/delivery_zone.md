# Техническое задание: Система тарифов доставки

## 0. Важно

- Документ описывает систему динамических тарифов доставки для модуля DeliveryZone.
- Система заменяет фиксированную стоимость доставки на гибкие ступенчатые тарифы.
- Все изменения обратно совместимы с существующей системой.

---

## 1. Описание и назначение

Система тарифов доставки позволяет:

- настраивать стоимость доставки в зависимости от суммы заказа;
- создавать многоуровневые тарифные планы для каждой зоны доставки;
- мотивировать клиентов увеличивать средний чек для снижения стоимости доставки;
- предоставлять бесплатную доставку при достижении определенной суммы заказа;
- автоматически пересчитывать стоимость доставки при применении бонусов.

Система используется:

- в Telegram Mini App (расчет стоимости доставки, отображение порогов);
- в админ-панели (управление тарифными планами);
- в бекенде заказов (определение итоговой стоимости доставки).

---

## 2. Основные понятия

### 2.1 Тарифная ступень (Delivery Tariff)

**Описание:** правило определения стоимости доставки для определенного диапазона суммы заказа.

**Ключевые параметры:**

- диапазон суммы заказа (от - до);
- стоимость доставки для этого диапазона;
- привязка к конкретному полигону доставки.

### 2.2 Тарифный план

**Описание:** набор тарифных ступеней для одного полигона доставки.

**Особенности:**

- один полигон может иметь от 1 до неограниченного количества ступеней;
- ступени не должны пересекаться или иметь пропуски;
- ступени автоматически сортируются по возрастанию суммы заказа.

### 2.3 Порог доставки

**Описание:** минимальная сумма заказа, при достижении которой меняется стоимость доставки.

---

## 3. Бизнес-правила

### 3.1 Структура тарифной ступени

Каждая тарифная ступень определяется тремя параметрами:

- **От (amount_from)** — минимальная сумма заказа для применения тарифа (включительно);
- **До (amount_to)** — максимальная сумма заказа для применения тарифа (включительно);
- **Стоимость доставки (delivery_cost)** — стоимость доставки для заказов в этом диапазоне.

**Примеры:**

```
От 0₽    До 399₽   → Доставка 299₽
От 400₽  До 799₽   → Доставка 199₽
От 800₽  До 899₽   → Доставка 99₽
От 900₽  До [пусто] → Доставка 0₽ (бесплатно)
```

**Правила диапазонов:**

- заказ на сумму 399₽ попадает в первый диапазон;
- заказ на сумму 400₽ попадает во второй диапазон;
- если поле "До" пустое — это последний диапазон, покрывающий все суммы выше;
- суммы округляются в меньшую сторону (399.50₽ = 399₽).

### 3.2 Валидация тарифного плана

При сохранении тарифного плана система проверяет:

**Обязательные проверки:**

- все диапазоны последовательны без пропусков;
- диапазоны не перекрываются;
- у каждого диапазона `amount_from < amount_to` (если `amount_to` указан);
- стоимость доставки неотрицательная (>= 0);
- первая ступень начинается с 0₽.

**Запрещенные ситуации:**

- пропуск диапазона: `0-399` и `500-799` (пропущен диапазон 400-499);
- перекрытие диапазонов: `0-500` и `400-800`;
- одинаковые диапазоны с разной стоимостью доставки;
- одинаковая стоимость доставки для разных диапазонов.

**Автоматические действия:**

- диапазоны автоматически сортируются по `amount_from` при сохранении.

### 3.3 Бесплатная доставка

Бесплатная доставка реализуется через установку `delivery_cost = 0` для соответствующей ступени.

**Пример:**
```
От 1000₽ До [пусто] → Доставка 0₽
```

Это означает: все заказы от 1000₽ и выше доставляются бесплатно.

### 3.4 Расчет стоимости доставки

**Алгоритм:**

1. Определяется итоговая сумма заказа (товары минус бонусы, если применены);
2. Сумма округляется в меньшую сторону;
3. Выбирается тарифная ступень, в диапазон которой попадает сумма заказа;
4. Возвращается соответствующая стоимость доставки.

**Примеры:**

- Заказ 350₽, тарифы: `0-399 → 299₽`, `400-799 → 199₽` → **Доставка 299₽**
- Заказ 400₽, те же тарифы → **Доставка 199₽**
- Заказ 1500₽, тарифы: `..., 1000-∞ → 0₽` → **Доставка 0₽**

### 3.5 Пересчет при применении бонусов

**Правило:** тариф доставки рассчитывается от итоговой суммы заказа ПОСЛЕ применения бонусов.

**Сценарий:**

1. Клиент формирует корзину на 800₽;
2. Система показывает: доставка 199₽ (по тарифу 400-799);
3. Клиент применяет 300 бонусов;
4. Итоговая сумма заказа: 500₽;
5. Система пересчитывает: доставка остается 199₽ (все еще диапазон 400-799);
6. Если бы клиент применил 400 бонусов, итоговая сумма стала бы 400₽, доставка по-прежнему 199₽.

**Предупреждение клиента:**

Если после списания бонусов стоимость доставки увеличивается:

**Было:**
- Корзина 500₽, доставка 199₽ (диапазон 400-799)

**Стало после применения 200 бонусов:**
- Итоговая сумма 300₽, доставка 299₽ (диапазон 0-399)

**Действие:** система показывает alert: "Внимание! После списания бонусов стоимость доставки изменится с 199₽ на 299₽"

**Важно:** клиент не подтверждает и не отменяет действие, а только получает информацию. Бонусы применяются независимо от изменения стоимости доставки.

### 3.6 Минимальная сумма заказа

**Правило:** заказы принимаются от 0₽.

Поле `min_order_amount` в полигоне доставки остается в системе, но устанавливается в значение 0₽ и не блокирует оформление заказов.

---

## 4. Управление тарифами в админ-панели

### 4.1 Создание тарифного плана

**Условия:**

- тарифный план создается для конкретного полигона доставки;
- можно создать план с нуля или скопировать из другого полигона.

**Процесс создания:**

1. Администратор открывает полигон в админ-панели;
2. Переходит на вкладку "Доставка";
3. Нажимает "Добавить ступень";
4. Заполняет поля: От, До, Стоимость доставки;
5. Повторяет шаг 3-4 для создания дополнительных ступеней;
6. Сохраняет тарифный план.

**Валидация при сохранении:**

- система автоматически сортирует ступени;
- проверяет отсутствие пропусков и перекрытий;
- показывает конкретные ошибки, если валидация не прошла;
- кнопка "Сохранить" неактивна при наличии ошибок.

### 4.2 Редактирование тарифного плана

**Доступные действия:**

- изменение параметров существующих ступеней;
- добавление новых ступеней;
- удаление ступеней;
- изменение порядка ступеней (через изменение диапазонов).

**Модалка редактирования:**

- открывается при нажатии кнопки "Редактировать тарифы";
- содержит таблицу со всеми ступенями;
- изменения применяются только после нажатия "Сохранить";
- при закрытии модалки без сохранения изменения отменяются.

**Удаление ступеней:**

- кнопка "Удалить" доступна для каждой ступени;
- удаление происходит без дополнительного подтверждения;
- при удалении средней ступени необходимо скорректировать соседние диапазоны для устранения пропуска.

### 4.3 Копирование тарифов

**Назначение:** быстрое создание идентичного тарифного плана для нескольких полигонов одного филиала.

**Условия копирования:**

- копировать можно только из полигонов того же филиала;
- копирование доступно только для полигонов без тарифов;
- в dropdown показываются только полигоны с настроенными тарифами.

**Процесс копирования:**

1. Администратор открывает полигон без тарифов;
2. Переходит на вкладку "Доставка";
3. Нажимает "Скопировать тарифы";
4. Выбирает полигон-источник из dropdown;
5. Просматривает preview: список ступеней, которые будут скопированы;
6. Подтверждает копирование;
7. Тарифы создаются как точные копии из источника.

**Ограничения:**

- если у полигона уже есть тарифы, кнопка "Скопировать тарифы" недоступна;
- копируются только тарифные ступени, остальные параметры полигона не изменяются.

### 4.4 Организация интерфейса

**Вкладка "Общая информация":**

- название полигона;
- филиал;
- статус (активен/неактивен);
- время доставки (delivery_time);
- минимальная сумма заказа (всегда 0₽);
- геометрия полигона (карта с возможностью редактирования точек).

**Вкладка "Доставка":**

- таблица тарифных ступеней;
- кнопка "Добавить ступень";
- кнопка "Скопировать тарифы" (если тарифов нет);
- кнопка "Редактировать тарифы" (если тарифы есть).

### 4.5 Обратная совместимость

**Поведение полигонов со старым форматом:**

Полигоны, у которых заполнено поле `delivery_cost` но нет тарифных ступеней, продолжают работать в старом формате:

- стоимость доставки фиксированная, не зависит от суммы заказа;
- в админ-панели во вкладке "Доставка" отображается текущая фиксированная стоимость;
- доступны кнопки для перехода на новую систему тарифов.

**Миграция на новую систему:**

Администратор может:
- создать тарифы с нуля;
- скопировать тарифы из другого полигона;
- создать один тариф на основе текущей фиксированной стоимости (диапазон 0-∞).

После создания тарифов поле `delivery_cost` игнорируется, используются тарифные ступени.

---

## 5. Визуализация для клиента

### 5.1 Отображение стоимости доставки

**Основная информация:**

В корзине клиент видит:
- текущую сумму заказа;
- текущую стоимость доставки для этой суммы.

**Пример:**
```
Ваш заказ: 650₽
Доставка: 199₽
```

### 5.2 Подсказки о порогах доставки

**Условия отображения:**

- подсказки показываются, если у полигона настроено 2 или более тарифных ступени;
- если только одна ступень (фиксированная стоимость) — подсказки не отображаются;
- подсказки показываются только в корзине.

**Содержание подсказок:**

Система показывает все следующие пороги, до которых клиент может "дотянуть" заказ для улучшения условий доставки.

**Пример (полный список):**
```
📊 Достигните выгодных условий:
• Добавьте товаров на 50₽ — доставка 199₽ (экономия 100₽)
• Добавьте товаров на 250₽ — доставка 99₽ (экономия 200₽)
• Добавьте товаров на 350₽ — бесплатная доставка! (экономия 299₽)
```

**Сокращенный список (при 6+ порогах):**

Если тарифных ступеней больше 5-6, показываются:
- первые 3 порога;
- многоточие "...";
- последний порог (обычно бесплатная доставка).

**Пример (сокращенный):**
```
📊 Достигните выгодных условий:
• Добавьте товаров на 50₽ — доставка 250₽
• Добавьте товаров на 150₽ — доставка 200₽
• Добавьте товаров на 300₽ — доставка 150₽
...
• Добавьте товаров на 800₽ — бесплатная доставка!
```

**Выделение бесплатной доставки:**

Последний порог с бесплатной доставкой выделяется визуально (иконка, жирный шрифт, другой цвет).

**Текущий порог достигнут:**

Если клиент уже в диапазоне бесплатной доставки:
```
🎉 У вас бесплатная доставка!
```

### 5.3 Динамический пересчет

**Поведение при изменении корзины:**

- при добавлении/удалении товаров стоимость доставки пересчитывается автоматически;
- подсказки о порогах обновляются в реальном времени;
- пересчет происходит локально на клиенте без запросов к серверу.

**Поведение при применении бонусов:**

1. Клиент вводит сумму бонусов к списанию;
2. Система рассчитывает новую итоговую сумму заказа;
3. Определяет новую стоимость доставки;
4. Если стоимость доставки увеличивается — показывает alert;
5. Бонусы применяются, доставка пересчитывается.

**Alert при списании бонусов:**

Показывается только когда:
- списание бонусов приводит к увеличению стоимости доставки;
- клиент переходит в диапазон с более дорогой доставкой.

**Формат alert:**
```
⚠️ Внимание!
После списания бонусов стоимость доставки изменится с 199₽ на 299₽

[Понятно]
```

Alert не блокирует применение бонусов, а только информирует клиента.

**Alert не показывается:**
- при добавлении товаров в корзину (даже если доставка дешевеет);
- если стоимость доставки не меняется после списания бонусов;
- если клиент отменяет применение бонусов до их списания.

---

## 6. API и интеграции

### 6.1 Проверка доступности доставки

**Endpoint:** `POST /api/polygons/check-delivery`

**Входные данные:**

- координаты адреса доставки (latitude, longitude);
- город (city_id);
- сумма корзины (cart_amount) — опционально.

**Выходные данные:**

Если доставка доступна:
- информация о полигоне;
- список всех тарифных ступеней полигона;
- текущая стоимость доставки (если передан cart_amount);
- информация о следующем пороге (сумма, стоимость, экономия).

Если доставка недоступна:
- сообщение о недоступности доставки по указанному адресу.

**Логика работы:**

1. Система определяет, в какой полигон попадают координаты;
2. Извлекает все тарифные ступени этого полигона;
3. Если передан `cart_amount`:
   - рассчитывает текущую стоимость доставки;
   - определяет следующий порог для отображения в подсказках;
4. Возвращает полную информацию клиенту.

### 6.2 Управление тарифами (административные методы)

**Получение тарифов полигона:**
- возвращает список всех тарифных ступеней;
- ступени отсортированы по `amount_from`.

**Сохранение тарифов:**
- принимает полный список ступеней;
- удаляет все существующие ступени полигона;
- создает новые на основе переданных данных;
- выполняет валидацию перед сохранением.

**Копирование тарифов:**
- принимает ID полигона-источника;
- копирует все ступени из источника в целевой полигон;
- проверяет, что целевой полигон в том же филиале;
- проверяет, что у целевого полигона нет тарифов.

### 6.3 Интеграция с модулем заказов

**При создании заказа:**

1. Определяется полигон доставки по адресу;
2. Рассчитывается сумма товаров в корзине;
3. Применяются бонусы (если клиент их использует);
4. Рассчитывается итоговая сумма заказа;
5. Определяется тарифная ступень по итоговой сумме;
6. Стоимость доставки фиксируется в заказе;
7. Рассчитывается общая сумма к оплате.

**Фиксация стоимости доставки:**

Стоимость доставки, рассчитанная на момент создания заказа, сохраняется в заказе и не изменяется при последующих изменениях тарифов полигона.

### 6.4 Интеграция с модулем лояльности

**При списании бонусов:**

1. Резервируются бонусы на счете клиента;
2. Рассчитывается новая итоговая сумма заказа (товары минус бонусы);
3. Пересчитывается стоимость доставки по новой итоговой сумме;
4. Если стоимость доставки изменилась — клиент получает уведомление;
5. Обновляется общая сумма заказа;
6. Бонусы списываются при подтверждении заказа.

**Важно:** 
- тариф рассчитывается от суммы товаров ПОСЛЕ вычета бонусов;
- пересчет происходит в реальном времени при изменении суммы бонусов.

---

## 7. Сценарии использования

### 7.1 Настройка тарифов для нового полигона

**Участники:** администратор

**Последовательность действий:**

1. Администратор создает новый полигон доставки в системе;
2. Переходит на вкладку "Доставка";
3. Выбирает один из вариантов:
   - Скопировать тарифы из другого полигона филиала;
   - Создать тарифы с нуля;
4. При создании с нуля:
   - нажимает "Добавить ступень";
   - заполняет поля: От 0₽, До 399₽, Доставка 299₽;
   - добавляет следующие ступени аналогично;
   - последнюю ступень создает с пустым полем "До";
5. Сохраняет тарифный план;
6. Система валидирует и сохраняет тарифы.

**Результат:** полигон готов к использованию с настроенными тарифами доставки.

### 7.2 Оформление заказа клиентом

**Участники:** клиент

**Последовательность действий:**

1. Клиент добавляет товары в корзину;
2. Переходит к оформлению заказа;
3. Вводит адрес доставки;
4. Система определяет полигон и показывает:
   - текущую стоимость доставки;
   - подсказки о следующих порогах (если есть);
5. Клиент видит, например: "Добавьте товаров на 100₽ — доставка будет 99₽";
6. Клиент может:
   - продолжить оформление с текущей доставкой;
   - добавить товары для достижения выгодного порога;
7. При достижении порога стоимость доставки автоматически пересчитывается;
8. Клиент завершает оформление заказа.

**Результат:** заказ создан с актуальной стоимостью доставки.

### 7.3 Применение бонусов с пересчетом доставки

**Участники:** клиент

**Последовательность действий:**

1. Клиент оформляет заказ на сумму 800₽;
2. Видит: "Доставка: 199₽" (диапазон 400-799₽);
3. Решает применить 300 бонусов;
4. Вводит сумму бонусов в соответствующее поле;
5. Система пересчитывает:
   - итоговая сумма заказа: 500₽;
   - стоимость доставки: 199₽ (остается в том же диапазоне);
6. Клиент подтверждает применение бонусов;
7. Заказ создается с учетом бонусов.

**Альтернативный сценарий (доставка дорожает):**

4. Клиент вводит 500 бонусов;
5. Система пересчитывает:
   - итоговая сумма заказа: 300₽;
   - новая стоимость доставки: 299₽ (диапазон 0-399₽);
6. Система показывает alert: "Внимание! После списания бонусов стоимость доставки изменится с 199₽ на 299₽";
7. Клиент закрывает alert и продолжает оформление;
8. Бонусы применяются, доставка пересчитана.

**Результат:** клиент информирован об изменении стоимости доставки, заказ создан корректно.

### 7.4 Массовое обновление тарифов филиала

**Участники:** администратор

**Последовательность действий:**

1. Администратор решает изменить тарифы для всех полигонов филиала;
2. Открывает один полигон, редактирует его тарифы;
3. Сохраняет изменения;
4. Открывает следующий полигон того же филиала;
5. Нажимает "Удалить все тарифы" (если есть);
6. Нажимает "Скопировать тарифы";
7. Выбирает первый полигон как источник;
8. Подтверждает копирование;
9. Повторяет шаги 4-8 для остальных полигонов филиала.

**Результат:** все полигоны филиала имеют идентичные тарифы доставки.

---

## 8. Критерии корректной работы

### 8.1 Backend

- тарифы корректно сохраняются и извлекаются из базы данных;
- расчет стоимости доставки точен для всех диапазонов;
- валидация корректно выявляет пропуски и перекрытия;
- автоматическая сортировка работает правильно;
- копирование тарифов создает точные копии;
- пересчет при бонусах выполняется корректно.

### 8.2 Admin Panel

- интерфейс управления тарифами интуитивен и удобен;
- валидация отображает понятные сообщения об ошибках;
- редактирование тарифов не вызывает потери данных;
- копирование работает только для допустимых полигонов;
- автосортировка не нарушает логику диапазонов.

### 8.3 Mini App

- подсказки о порогах отображаются корректно;
- пересчет доставки происходит в реальном времени;
- alert при бонусах показывается только когда необходимо;
- визуализация порогов не перегружает интерфейс;
- все расчеты точны и соответствуют серверным данным.

### 8.4 Интеграции

- модуль заказов корректно фиксирует стоимость доставки;
- модуль лояльности корректно пересчитывает тариф при бонусах;
- API возвращает полные и корректные данные;
- обратная совместимость со старым форматом сохранена.

---

## 9. Ограничения и особенности

### 9.1 Технические ограничения

- минимальная сумма диапазона: 0₽;
- максимальная сумма диапазона: не ограничена;
- количество тарифных ступеней: не ограничено;
- валюта: только рубли (₽).

### 9.2 Бизнес-ограничения

- тарифы привязываются только к полигонам, не к филиалам или городам;
- копирование доступно только между полигонами одного филиала;
- копирование доступно только для полигонов без тарифов;
- изменение тарифов не влияет на уже созданные заказы.

### 9.3 UX особенности

- подсказки показываются только при наличии 2+ тарифов;
- alert при бонусах показывается только при ухудшении условий;
- пересчет доставки происходит автоматически без действий клиента;
- округление сумм всегда в меньшую сторону.

---

## 10. Будущие улучшения (не входят в текущую реализацию)

- история изменений тарифов с указанием автора и времени;
- аналитика эффективности тарифных планов;
- A/B тестирование различных тарифных стратегий;
- шаблоны тарифов для быстрой настройки;
- массовое обновление тарифов для нескольких полигонов;
- импорт/экспорт тарифных планов;
- уведомления менеджеров о приближении к порогам бесплатной доставки;
- динамические тарифы в зависимости от времени суток или дня недели.

---

**Конец документа**