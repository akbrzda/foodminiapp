# 🎯 FoodMiniApp

> Единая система онлайн‑заказа еды с Telegram Mini App, админ‑панелью и backend API для управления меню, заказами и доставкой.

![Status](https://img.shields.io/badge/status-MVP-yellow) ![Stack](https://img.shields.io/badge/stack-Vue3%20%7C%20Node.js-blue) ![DB](https://img.shields.io/badge/db-MySQL8%20%2B%20Redis-informational) ![License](https://img.shields.io/badge/license-ISC-lightgrey)

## 🧭 О проекте

FoodMiniApp закрывает полный цикл заказа еды в Telegram: от выбора блюд и оформления доставки до управления операциями кухни в админ‑панели. Система рассчитана на рестораны и сети, которым важно запускаться быстро и работать стабильно в пиковых нагрузках. Ключевое преимущество — связка Telegram Mini App + real‑time админ‑панели с геозонами и модульной архитектурой бэкенда. Проект нацелен на масштабирование по городам и филиалам.

## ⭐ Key Features

- **Telegram Mini App** — заказ внутри Telegram без установки отдельного приложения.
- **Гибкое меню** — категории, позиции, варианты, модификаторы, теги и стоп‑лист.
- **Доставка по полигонам** — проверка адресов через PostGIS, тарифные ступени и геокодинг.
- **Корзина и заказы** — расчет, оформление, статусы, повтор заказа и история.
- **Лояльность** — 3 уровня, начисления/списания, история транзакций и аудит.
- **Админ‑панель real‑time** — управление заказами, сменой, пользователями и настройками через WebSocket.
- **Changelog и релизы** — версионирование `MAJOR.MINOR.PATCH`, публикация релизов и отображение “Что нового”.
- **Маркетинговые рассылки** — сегменты, кампании, очередь, статистика и конверсии.
- **Background Workers** — автоматизация через BullMQ: обработка изображений, рассылки, автостатусы заказов, бонусы.
- **Миграции БД** — версионирование схемы, автоматическая синхронизация с schema.sql.

## 🧰 Tech Stack

- **Frontend:** Vue 3, Vite, Pinia, Vue Router, Tailwind CSS, Shadcn UI, Radix Vue, Lucide.
- **Admin UI:** Leaflet, Leaflet.draw, Unovis, vue-sonner.
- **Backend:** Node.js, Express, WebSocket, BullMQ, Axios, Winston.
- **DB/Cache:** MySQL 8 + PostGIS, Redis.
- **Инфраструктура:** Docker Compose (локально), Linux, Apache, pm2.
- **Интеграции:** Telegram Bot API, Telegram Mini App SDK, Nominatim (OpenStreetMap).

## 🔌 Интеграции iiko + PremiumBonus (MVP)

Реализована базовая платформа интеграций с режимом `local-first`:

- Настройки интеграций через `system_settings`:
  - `iiko_enabled`, `iiko_api_url`, `iiko_api_token`, `iiko_sync_category_ids`
  - `iiko_external_menu_id`, `iiko_webhook_secret`
  - `premiumbonus_enabled`, `premiumbonus_api_url`, `premiumbonus_api_token`, `premiumbonus_sale_point_id`
  - `integration_mode` (`menu/orders/loyalty`)
  - для меню используются все организации, доступные по API iiko (без ручного выбора организации)
- Локальная буферизация в БД:
  - `orders`: `iiko_*` и `pb_*` статусы синхронизации
  - `users`: `pb_*` статусы синхронизации, `loyalty_mode`
  - `menu_*`: внешние `iiko_*_id`
- Очереди BullMQ:
  - `sync:iiko:menu`, `sync:iiko:stoplist`, `sync:iiko:orders`
  - `sync:premiumbonus:clients`, `sync:premiumbonus:purchases`
- Webhooks:
  - `POST /api/webhooks/iiko/order-status`
  - `POST /api/webhooks/iiko/stoplist`
- Админ API интеграций:
  - `GET/PUT /api/admin/integrations/settings`
  - `POST /api/admin/integrations/iiko/test-connection`
  - `POST /api/admin/integrations/premiumbonus/test-connection`
  - `POST /api/admin/integrations/iiko/sync-menu`
  - `POST /api/admin/integrations/iiko/sync-stoplist`
  - `GET /api/admin/integrations/iiko/sync-status`
  - `GET /api/admin/integrations/sync-logs`
  - `POST /api/admin/integrations/retry-failed`
  - `POST /api/admin/integrations/retry-entity`

Ограничения read-only при активной интеграции iiko:
- Блокируются изменяющие admin-эндпоинты меню и полигонов доставки (возвращается `403`).

Системные особенности:

- Номер заказа циклический `0001–9999`.
- Смена: 05:00–04:59 по локальной таймзоне города.
- Тарифы доставки рассчитываются по ступеням суммы заказа.
- Кеши и очереди на Redis, фоновые задачи через BullMQ.
- WebSocket для real-time синхронизации между админкой и клиентами.
- Логирование всех операций с системой мониторинга.

## 📝 Changelog и версии

Реализован code-driven changelog: релизы хранятся в JSON-конфиге и обновляются вместе с кодом.

- Источник релизов: `backend/src/config/changelog.json`
- Public API (read-only):
  - `GET /api/changelog/latest`
  - `GET /api/changelog/releases`
  - `GET /api/changelog/releases/:id`

Документация модуля: `docs/changelog.md`.

## 🛠️ Сервисные скрипты

### Выгрузка меню и вариантов в CSV для обновления цен

В корне проекта доступен скрипт `export.cjs`, который выгружает цены для одного выбранного города в CSV.

Что попадает в CSV:
- `item_id`, `item_name_with_variant` (название с размером в скобках), `variant_id`, `city_id`.
- `base_price` (базовая цена варианта или позиции).
- `delivery_price` и `pickup_price` отдельными столбцами в одной строке.

Запуск:

```bash
node export.cjs --city-id=13
```

С указанием пути файла:

```bash
node export.cjs --city-id=13 --out=./exports/menu-prices-city-13.csv
```

По умолчанию файл создается в корне проекта: `menu-prices-export.csv`.

Требования:
- Должно быть доступно подключение к MySQL.
- Скрипт читает настройки из `backend/.env` (приоритетно) и затем из корневого `.env`.

### Импорт цен из CSV

В корне проекта доступен скрипт `import.cjs`, который импортирует цены из CSV (формат, который генерирует `export.cjs`).

Что делает импорт:
- Обрабатывает один город за запуск (CSV должен содержать один `city_id`).
- Для каждой строки обновляет цены каналов `delivery` и `pickup`.
- Если `variant_id` заполнен: обновляет `menu_variant_prices`.
- Если `variant_id` пустой: обновляет `menu_item_prices`.

Запуск:

```bash
node import.cjs --file=./menu-prices-export.csv
```

Проверка без сохранения в БД:

```bash
node import.cjs --file=./menu-prices-export.csv --dry-run
```

---

## 📦 Модульная структура

### Backend Modules

#### 🔐 Auth Module

Модуль авторизации и аутентификации пользователей.

- Короткоживущий access token + ротация refresh token (`/api/auth/refresh`).
- Валидация JWT по `issuer/audience/algorithm` на backend.
- Авторизация через номер телефона Telegram.
- Logout с очисткой серверной cookie сессии и blacklist токена.
- Middleware для проверки ролей и доступа к эндпоинтам.
- Разграничение доступа: admin, ceo, manager, user.
- Хеширование паролей через bcrypt для админов.
- Логирование всех входов и попыток авторизации.

#### 📍 Delivery Module

Управление городами, филиалами и зонами доставки.

- CRUD городов с координатами и часовыми поясами.
- CRUD филиалов с привязкой к городам.
- Полигоны доставки через PostGIS (ST_Contains, ST_GeomFromGeoJSON).
- Проверка попадания адреса в зону доставки.
- Ступенчатые тарифы доставки в зависимости от суммы заказа.
- Кеширование списка городов и филиалов в Redis (TTL: 1 час).

#### 🌍 Geocode Module

Геокодирование адресов через OpenStreetMap Nominatim API.

- Прямое геокодирование: адрес → координаты.
- Обратное геокодирование: координаты → адрес.
- Автокомплит адресов с фильтрацией по городу.
- Rate limiting для соблюдения лимитов Nominatim (1 req/sec).
- Кеширование результатов в Redis (TTL: 24 часа).
- Нормализация адресов для единообразия.

#### 🍕 Menu Module

Управление меню ресторана: категории, позиции, варианты, модификаторы.

- CRUD категорий меню с сортировкой и активностью.
- CRUD позиций меню с изображениями, описанием, ценами.
- Варианты позиций (размеры) с разными ценами и собственными изображениями (`fallback` на базовое фото позиции).
- Модификаторы: одиночный/множественный выбор, обязательные/опциональные.
- Индивидуальные цены модификаторов по конкретным вариантам (`menu_modifier_variant_prices`) только для тех блюд/вариантов, где модификатор подключен.
- Теги для фильтрации (острое, вегетарианское, хит).
- Стоп-лист: временное отключение позиций с причиной.
- Кеширование меню по городам в Redis (TTL: 5 минут).

#### 🛒 Orders Module

Полный цикл работы с заказами от создания до завершения.

- Создание заказа с валидацией состава корзины.
- Расчет цен позиций и вариантов через `menu_item_prices` / `menu_variant_prices` с учетом `city_id` и `fulfillment_type`.
- Применение цены модификатора по `variant_id` (если настроено), иначе используется базовая цена модификатора.
- Проверка доступности позиций в городе через `menu_item_cities`.
- Автоматический расчет стоимости доставки по тарифам.
- Для внутренних backend-вызовов геосервисов поддерживается `INTERNAL_API_URL` (fallback для локального `localhost` без HTTPS-конфликта).
- Циклический номер заказа (0001-9999) с инкрементом в БД.
- Статусы: pending, confirmed, preparing, ready, delivering, completed, cancelled.
- Применение и начисление бонусов при оформлении/завершении.
- История заказов пользователя с фильтрацией и пагинацией.
- Real-time уведомления через WebSocket при смене статуса.
- Автоматическая отмена просроченных заказов через воркер.

#### 💰 Loyalty Module

Гибкая система лояльности с неограниченным числом уровней.

- Управление уровнями: название, порог, процент начисления/списания.
- Автоматический расчет уровня по сумме заказов за период (по умолчанию 60 дней).
- Начисление бонусов после завершения заказа по формуле уровня.
- Списание бонусов при оформлении с проверкой лимитов.
- Триггерные начисления: регистрация, день рождения.
- Автоматическое истечение бонусов по сроку через воркер.
- История транзакций с аудитом: кто, когда, за что.
- Деградация уровня при отсутствии активности (опционально).

#### 👥 Users Module

Управление клиентами и их профилями.

- CRUD пользователей с полями: телефон, имя, email, дата рождения.
- Нормализация и сохранение телефона в формате E.164 (`+7...`) для корректной уникальности и поиска.
- Управление адресами доставки (множественные адреса, адрес по умолчанию).
- Просмотр баланса бонусов и текущего уровня лояльности.
- История заказов пользователя с детализацией.
- Детальная статистика клиента: количество заказов, средний чек, общая сумма.
- Поиск и фильтрация клиентов в админ-панели.
- Самоудаление аккаунта пользователем через Mini App (`DELETE /api/users/me`).

#### 🏢 Admin Module

Управление администраторами и их правами доступа.

- CRUD администраторов с ролями: admin, ceo, manager.
- Привязка менеджеров к конкретным городам (ограничение видимости).
- Смена пароля и email для администраторов.
- Логирование всех действий администраторов (audit trail).
- Многоуровневый доступ к функционалу в зависимости от роли.

#### 📊 Analytics Module

Базовая аналитика и отчетность по заказам.

- Статистика по сменам: количество заказов, выручка, средний чек.
- Топ-позиции меню по продажам.
- Распределение заказов по типам (доставка/самовывоз).
- Распределение заказов по статусам.
- Динамика заказов по часам/дням/месяцам.
- Экспорт отчетов в форматах JSON/CSV.

#### 📢 Broadcasts Module

Маркетинговые рассылки через Telegram с сегментацией.

- Создание рассылок с текстом, изображениями, кнопками.
- Сегментация аудитории по множественным критериям (AND/OR).
- Сохраненные сегменты для повторного использования.
- Планирование отправки по расписанию с учетом часовых поясов.
- Триггерные рассылки: спящие клиенты, день рождения, новая регистрация.
- Очередь отправки через BullMQ (30 сообщений/сек для Telegram).
- Статистика: отправлено, доставлено, прочитано, переходы по ссылкам.
- Трекинг конверсий: заказы после рассылки.

#### 🗺️ Polygons Module

Управление полигонами зон доставки через PostGIS.

- CRUD полигонов с привязкой к филиалам.
- Визуальный редактор полигонов на карте Leaflet.
- Проверка пересечений полигонов разных филиалов.
- Расчет площади зоны доставки.
- Импорт полигонов из GeoJSON с предпросмотром и подтверждением сохранения.
- Экспорт полигонов филиала в GeoJSON.
- Валидация геометрии полигонов (замкнутость, самопересечения).

#### ⚙️ Settings Module

Централизованное управление настройками системы.

- Глобальные настройки: часовые пояса, валюта, таймауты.
- Настройки лояльности: включение/выключение, срок бонусов, период расчета уровня.
- Настройки заказов: минимальная сумма, время автоотмены, рабочие часы.
- Настройки уведомлений: Telegram bot token, chat ID для уведомлений персонала.
- Переменная окружения `TELEGRAM_MINIAPP_URL` для формирования кнопки перехода в Mini App из Telegram-уведомлений о статусе заказа.
- Командный Telegram-бот backend работает через long polling (`getUpdates`, без webhook), поддерживает команду `/start` и показывает кнопку `Открыть приложение` в нижней клавиатуре Telegram (`reply keyboard web_app`).
- Переменная окружения `TELEGRAM_COMMAND_BOT_ENABLED` управляет запуском командного бота (`true` по умолчанию).
- Настройки доставки: тарифы по умолчанию, минимальная сумма для бесплатной доставки.
- Динамическое обновление настроек без перезапуска сервера.

#### 📤 Uploads Module

Загрузка и обработка изображений.

- Загрузка изображений для позиций меню и рассылок.
- Автоматическая обработка через воркер: resize, crop, оптимизация.
- Сохранение в оптимизированном формате WebP для отображения в клиентах.
- Имя файла включает идентификатор сущности (entity id) для удобной трассировки.
- Генерация thumbnail и разных размеров (small, medium, large).
- Валидация типов файлов и размеров (max 10MB).
- Валидация идентификаторов сущностей и детальные ответы API при ошибках загрузки.
- Хранение в локальной файловой системе с уникальными именами.
- Удаление связанных изображений при удалении сущности.

#### 📜 Logs Module

Централизованное логирование всех событий системы.

- Логирование действий администраторов с деталями (кто, что, когда).
- Логирование системных событий: старт, остановка, ошибки.
- Логирование заказов: создание, смена статуса, отмена.
- Фильтрация логов по типу, уровню, дате, пользователю.
- Хранение логов в БД с ротацией (архивация старых логов).
- Winston для структурированного логирования.

---

### Admin Panel Modules

#### 📊 Dashboard Module

Главная панель с ключевыми метриками.

- Виджеты: заказы сегодня, выручка, средний чек, активные заказы.
- Графики динамики заказов по часам и дням.
- Топ-позиции меню текущего дня.
- Быстрый доступ к активным заказам.
- Real-time обновление метрик через WebSocket.

#### 📋 Orders Module

Управление заказами с детализацией и историей.

- Список заказов с фильтрацией по статусам, датам, филиалам.
- Детальная карточка заказа: состав, клиент, доставка, история статусов.
- Смена статуса заказа с подтверждением.
- Telegram-уведомления клиенту о смене статуса содержат кнопку `Открыть заказ` (web_app) с переходом в Mini App на детали заказа.
- Печать/экспорт заказа для кухни.
- Звуковые уведомления о новых заказах.
- Страница "Текущая смена" с картой и упрощенным управлением.

#### 👥 Clients Module

База клиентов с детальной информацией.

- Список клиентов с поиском по телефону, имени.
- Детальная карточка клиента: заказы, бонусы, адреса, статистика.
- Редактирование профиля клиента (имя, email, дата рождения).
- Ручное начисление/списание бонусов с причиной.
- История транзакций бонусов.
- Блокировка/разблокировка клиента.
- Удаление клиента из карточки в админ-панели (`DELETE /api/admin/clients/:id`).

#### 🍕 Menu Module

Полное управление меню ресторана.

- CRUD категорий с сортировкой drag-and-drop.
- CRUD позиций меню с загрузкой изображений.
- В списке позиций отображается минимальная доступная цена по позиции.
- Управление вариантами (размеры) и модификаторами.
- Теги и фильтры для позиций.
- Стоп-лист: массовое отключение позиций с причиной и временем.
- Копирование позиций между категориями.

#### 📍 Delivery Module

Управление географией доставки.

- CRUD городов и филиалов.
- Редактор полигонов зон доставки на интерактивной карте Leaflet.
- Управление тарифами доставки (ступени по сумме заказа).
- Проверка адреса на попадание в зону.
- Визуализация всех полигонов на карте.

#### 📢 Broadcasts Module

Маркетинговые рассылки с дашбордом и статистикой.

- Список рассылок с фильтрацией по статусу, дате.
- Создание рассылки: текст, изображение, кнопки, сегменты.
- Сохраненные сегменты для быстрого повторного использования.
- Планирование отправки по дате и времени.
- Тестовая отправка на свой номер перед запуском.
- Дашборд статистики: отправлено, доставлено, прочитано, клики, конверсии.

#### ⚙️ System Module

Настройки системы и администрирование.

- Управление глобальными настройками (часовые пояса, валюта).
- Управление настройками лояльности, заказов, уведомлений.
- Список администраторов с управлением ролями и доступом.
- Логи системы с фильтрацией и поиском.
- Информация о системе: версия, uptime, подключения.

#### 🔐 Auth Module

Авторизация администраторов в панели.

- Вход по email и паролю.
- Восстановление пароля через email.
- Хранение JWT-токенов админ-панели в sessionStorage (с миграцией из legacy localStorage).
- Автоматическое обновление access token через `/api/auth/refresh` при `401/403`.
- Выход с серверным logout, очисткой cookie и blacklist токена.

---

### Telegram Mini App Modules

#### 🔐 Auth Module

Авторизация пользователей через Telegram.

- Первая авторизация запускается пользователем вручную кнопкой на экране входа.
- Запрос номера телефона через встроенную кнопку Telegram.
- Регистрация нового пользователя при первом входе.
- Access token сохраняется локально (`localStorage`) и дублируется в Telegram CloudStorage для устойчивого восстановления сессии.
- При старте приложения сначала проверяется локальный access token, затем `HttpOnly` refresh cookie (`/api/auth/refresh`).
- Для пользователей с историей входа включен тихий fallback на `/api/auth/telegram` через `initData`, если access/refresh недоступны.
- Обновленный клиентский TTL access token: 2 часа; refresh token: 30 дней.
- Валидация существования пользователя в БД при старте приложения с авторазлогином при удаленной учетке.

#### 🍕 Menu Module

Просмотр меню и деталей позиций.

- Главная страница с категориями и позициями меню.
- Фильтрация по тегам (острое, вегетарианское, хит).
- Детальная карточка позиции: описание, фото, цена, варианты, модификаторы.
- Выбор вариантов (размеры) и модификаторов перед добавлением в корзину.
- Поиск позиций по названию.
- Кеширование меню для офлайн-доступа.

#### 🛒 Cart Module

Корзина и оформление заказа.

- Добавление/удаление позиций из корзины.
- Изменение количества позиций.
- Автоматический расчет суммы заказа с учетом модификаторов и вариантов.
- Применение бонусов с проверкой лимитов.
- Выбор типа заказа: доставка или самовывоз.
- Страница Checkout: адрес, время, способ оплаты, комментарий.
- Валидация перед отправкой заказа.

#### 📍 Location Module

Управление городом, филиалом и адресами доставки.

- Выбор города при первом запуске (автоопределение или вручную).
- Выбор филиала для заказа.
- CRUD адресов доставки с геокодированием.
- Проверка попадания адреса в зону доставки.
- Выбор адреса по умолчанию.
- Автокомплит адресов через backend-геокодинг (`/api/polygons/geocode`) с выдачей списка кандидатов.
- Выбор точки доставки через draggable-маркер Leaflet и клик по карте с обратным геокодированием.

#### 💰 Loyalty Module

Отображение бонусов и уровня лояльности.

- Текущий баланс бонусов и уровень на главном экране.
- Детальная страница программы лояльности: описание уровней, процент начисления/списания.
- История транзакций бонусов с фильтрацией.
- Уведомления о начислении/списании бонусов.
- Прогресс до следующего уровня (визуальный индикатор).

#### 📦 Orders Module

История заказов и их детали.

- Список всех заказов пользователя с пагинацией.
- Фильтрация по статусам (активные, завершенные, отмененные).
- Детальная карточка заказа: состав, адрес, статус, стоимость.
- Отслеживание статуса заказа в реальном времени.
- Повтор заказа одной кнопкой.
- Отмена заказа (если статус позволяет).

#### 👤 Profile Module

Профиль пользователя и настройки.

- Просмотр и редактирование профиля: имя, email, дата рождения.
- Управление адресами доставки.
- Просмотр истории заказов.
- Баланс бонусов и текущий уровень лояльности.
- Выход из аккаунта.

#### ⚙️ Settings Module

Настройки приложения.

- Выбор темы (светлая/темная/системная).
- Уведомления о статусе заказа (вкл/выкл).
- Язык интерфейса (если поддерживается мультиязычность).
- Информация о приложении: версия, политика конфиденциальности.

#### 🧭 Navigation & Layout

- Единый `PageHeader` используется на экранах Mini App, где нет `AppHeader`.
- Верхний отступ интерфейса учитывает safe area (`env(safe-area-inset-top)` и Telegram inset-переменные).
- Контентные экраны Mini App используют единый контейнер `page-container` с базовыми отступами `16px 12px`.
- Action-кнопки Mini App (`Сохранить`, `Удалить`, `Выйти`, `Оформить`, `Перейти`, `Добавить в корзину`) используют единый стиль `action-btn` и цветовые варианты `btn-primary`/`btn-secondary`/`btn-danger`.

---

## 🔄 Background Workers

### 📧 Telegram Worker (BullMQ)

- Отправка уведомлений клиентам через Telegram Bot API.
- Обработка очереди сообщений с приоритетами.
- Ретраи при ошибках отправки (до 3 попыток).
- Логирование всех отправленных сообщений.

### 🖼️ Image Worker (BullMQ)

- Обработка загруженных изображений: resize, crop, оптимизация.
- Генерация thumbnail и разных размеров (small, medium, large).
- Конвертация форматов (WebP для оптимизации).
- Удаление оригиналов после обработки (опционально).

### ⏰ Order Auto-Status Worker (Cron)

- Автоматическая отмена просроченных заказов (pending > 15 минут).
- Перевод заказов в статус "Просрочен" если не доставлены вовремя.
- Уведомления администраторам о просроченных заказах.

### 💰 Bonus Expiry Worker (Cron)

- Проверка истечения срока действия бонусов (каждый день в 00:00).
- Автоматическое списание истекших бонусов.
- Уведомления клиентам о скором истечении бонусов (за 3 дня).

### 🎂 Birthday Bonus Worker (Cron)

- Проверка дней рождения клиентов (каждый день в 00:00).
- Начисление бонусов за день рождения (за 3 дня до ДР).
- Отправка поздравительных сообщений.

### 📢 Broadcast Worker (BullMQ)

- Обработка очереди рассылок с соблюдением лимитов Telegram (30 сообщений/сек).
- Пакетная отправка сообщений по сегментам.
- Обновление статистики рассылки в реальном времени.
- Логирование доставки и ошибок.
- Если таблицы рассылок еще не применены в БД (например, нет `broadcast_queue`), воркер автоматически отключается до следующего рестарта после миграции.

### 🎯 Trigger Worker (Cron)

- Проверка условий триггерных рассылок (каждый час).
- Запуск рассылок для спящих клиентов (не заказывали N дней).
- Автоматическое создание задач в Broadcast Worker.

---

## 🔌 Real-time Communication (WebSocket)

### Архитектура WebSocket-сервера

- Авторизация через одноразовый `ws-ticket` (`POST /api/auth/ws-ticket`, TTL 45 сек).
- Комнатная модель (rooms): admin-orders, city-{id}-orders, user-{id}.
- Heartbeat (ping/pong) для проверки активности соединений.
- Автоматическое переподключение на клиенте при обрыве.

### События для админ-панели

- `order:new` — новый заказ (звуковое уведомление).
- `order:updated` — изменение статуса заказа.
- `broadcast:progress` — прогресс рассылки в реальном времени.
- `system:notification` — системные уведомления (ошибки, предупреждения).

### События для клиентов (Mini App)

- `order:status` — изменение статуса заказа клиента.
- `bonus:transaction` — начисление/списание бонусов.
- `level:upgraded` — повышение уровня лояльности.

---

## 🔐 Актуальные меры безопасности Mini App (обновлено: 10 февраля 2026)

- Access token в Mini App хранится только в оперативной памяти клиента.
- Убраны клиентские override-каналы для Telegram `initData` (`window.__telegram*`, `sessionStorage tg_init_data*`).
- Удален monkey-patch Telegram SDK событий в экране логина.
- Пользовательские уведомления на критичных сценариях переведены на Telegram-совместимый `showAlert`.
- Для динамически загружаемых ресурсов (Leaflet, Eruda) добавлены `integrity` (SRI) и `crossorigin`.
- Добавлен CSP-meta в `telegram-miniapp/index.html` для снижения рисков инъекций.
- На backend добавлена адаптивная cookie-конфигурация auth (`AUTH_COOKIE_SAMESITE`, `AUTH_COOKIE_SECURE`) для стабильной работы Mini App.
- Лимитатор `/api/auth/telegram` смягчен для UX (успешные входы не приводят к преждевременной блокировке после нескольких запусков).
- CORS разрешает запросы без заголовка `Origin` (мониторинги, боты, server-to-server), при этом браузерные origin-проверки остаются активными.
- JSON parser backend настроен в режиме `strict: false`, поэтому payload `null` на `/api/auth/refresh` больше не приводит к `SyntaxError` в логах.
